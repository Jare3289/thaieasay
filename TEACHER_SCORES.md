# ตารางคะแนนที่ครูเป็นผู้ประเมิน

หน้า **ตารางคะแนนครูประเมิน** (`teacher_scores.php`) รวบรวม **คะแนนที่ครูเป็นผู้ประเมินเท่านั้น**
มาทำเป็นตารางสำหรับตัดสินผลการเรียนและรายงานผลวิจัย พร้อมบอกว่าเหลืองานชิ้นไหนที่ครูยังไม่ได้ตรวจ

เปิดได้จาก **เมนูข้าง → ตารางคะแนนครูประเมิน** (ครูเท่านั้น)

| ไฟล์ | หน้าที่ |
|---|---|
| `teacher_scores.php`      | หน้าเว็บ — ตารางทั้งหมด รายการที่ยังไม่ตรวจ ส่งออก CSV และพิมพ์ |
| `teacher_scores_data.php` | ชั้นข้อมูล — ดึงคะแนน จับคู่รอบ และคำนวณสถิติ (อ่านอย่างเดียว) |

---

## หลักการที่ใช้ออกแบบ

**1. ใช้คะแนนของครูอย่างเดียว**
ทุกตัวเลขในหน้านี้มาจากแถวที่ `evaluations.evaluator_type = 'ครูประเมิน'` เท่านั้น
คะแนนประเมินตนเอง คะแนนจากเพื่อน และคะแนนของผู้เชี่ยวชาญ **ไม่ถูกนำมารวมแม้แต่ค่าเดียว**
เพราะเป็นข้อมูลประกอบ ไม่ใช่คะแนนที่ใช้ตัดสินผลสัมฤทธิ์

**2. คู่ที่จับกันไว้ตายตัว**

| คู่ | รอบแรก | รอบหลัง | ใช้อ่านอะไร |
|---|---|---|---|
| คู่ที่ 1 | ก่อนเรียน (`pretest`) | หลังเรียน (`posttest`) | พัฒนาการจากการจัดการเรียนการสอนทั้งหมด |
| คู่ที่ 2 | หน่วยที่ 1 ร่างที่ 2 (`task1`) | หน่วยที่ 2 ร่างที่ 2 (`task2`) | การเปลี่ยนแปลงระหว่างภาระงานสองหน่วย |

ตรงกับคู่เทียบที่ระบบใช้อยู่แล้วในหน้าประเมิน (`comparisonPhaseMap` ใน `evaluation.php`)
และคู่ฉบับตั้งต้นของระบบตรวจอัตโนมัติ (`ai_baseline_pairs()` ใน `writing_check_config.php`)

**3. สองคู่คิดสถิติแยกกันคนละชุด**
แต่ละคู่ใช้เฉพาะนักเรียนที่มีคะแนน **ครบทั้งสองรอบของคู่นั้น**
เช่นนักเรียนที่ครูตรวจก่อนเรียน–หลังเรียนครบ แต่ยังไม่ได้ตรวจหน่วยที่ 2
จะถูกนับในคู่ที่ 1 แต่ไม่ถูกนับในคู่ที่ 2 ค่า **n ของสองคู่จึงไม่จำเป็นต้องเท่ากัน**
ระบบแสดง n ที่ใช้จริงกำกับไว้ทุกตาราง ไม่มีการเติมค่าแทนคะแนนที่ยังไม่มี

**4. ภาระงานให้คะแนนเฉพาะร่างที่ 2**
แต่ละหน่วยมีเรียงความสองร่าง (`task1_d1` / `task1_d2`) แต่ครูให้คะแนนเฉพาะร่างที่ 2
ตามที่ `evaluation.php` กำหนดไว้ (`gradingEssayPhase`) คะแนนจึงถูกบันทึกในรอบ `task1` / `task2`
โดยอ่านจากเรียงความ `task1_d2` / `task2_d2`

**5. ยังไม่ตรวจ ≠ ได้ 0 คะแนน**
ช่องที่ครูยังไม่ได้ให้คะแนนจะขึ้นว่า **ยังไม่ตรวจ** (กดไปให้คะแนนได้ทันที)
ไม่ใช่เลข 0 และไม่ถูกนับเข้าค่าเฉลี่ยหรือค่า n ใด ๆ
ส่วนงานที่นักเรียนยังไม่ส่ง จะแยกไว้เป็น **ยังไม่ส่งงาน** เพราะยังตรวจไม่ได้ ต้องตามงานก่อน

---

## หน้านี้มีอะไรบ้าง

### 0. งานที่ครูยังไม่ได้ตรวจ (ขึ้นก่อนทุกตาราง)

- นับให้รายรอบว่า **ตรวจแล้วกี่คนจากกี่คน** และเหลือรอตรวจกี่ชิ้น
- รายชื่อ **"ส่งงานแล้ว รอครูตรวจ"** พร้อมปุ่ม *ให้คะแนน* ที่ลิงก์ตรงไปยัง
  `evaluation.php?mode=teacher&phase=<รอบ>&student=<รหัส>` — เปิดแล้วได้ฟอร์มของคนนั้นในรอบนั้นทันที
- รายชื่อ **"ยังไม่ส่งงาน จึงยังตรวจไม่ได้"** แยกไว้ต่างหาก
- รายชื่อ **"ส่งแล้วแต่ระบบยังไม่ได้ตรวจ"** สั่งตรวจต่อได้ที่หน้าระบบตรวจอัตโนมัติ

ถ้ายังตรวจไม่ครบ ระบบขึ้นเตือนไว้เหนือตารางว่า *ค่าเฉลี่ยและค่า n ยังไม่ใช่ตัวเลขสุดท้าย*

### ตารางที่ 1 — คะแนนครูประเมินรายบุคคล

คะแนนรวมเต็ม 60 ทั้ง 4 รอบ พร้อมผลต่างสองคู่รายคน ระดับคุณภาพหลังเรียน
และแถวท้ายตาราง: n · ค่าเฉลี่ย (M) · ส่วนเบี่ยงเบนมาตรฐาน (SD) · ต่ำสุด · สูงสุด ของแต่ละรอบ

> แถวท้ายตารางนี้คิดจาก **ทุกคนที่ครูตรวจแล้วในรอบนั้น** (ยังไม่บังคับว่าต้องครบคู่)
> ใช้อ่านภาพรวมรายรอบ ส่วนตัวเลขที่ใช้ทดสอบสมมติฐานให้ดูตารางที่ 2

### ตารางที่ 2.1 และ 2.2 — ผลการเปรียบเทียบแบบจับคู่ (คะแนนครู)

หนึ่งตารางต่อหนึ่งคู่ แต่ละตารางมีแถวภาพรวมและแถวราย 4 องค์ประกอบ
คอลัมน์: คะแนนเต็ม · n · M และ SD ของทั้งสองรอบ · ผลต่างเฉลี่ย · SD ของผลต่าง ·
t · df · p · Cohen's d<sub>z</sub> · ขนาดอิทธิพล

ใต้ตารางมีย่อหน้าอ่านผล และผลทดสอบ **Shapiro-Wilk** ของคะแนนผลต่าง
ถ้าคะแนนผลต่างต่างจากการแจกแจงปกติ ระบบเตือนให้รายงานอย่างระมัดระวัง
หรือเพิ่ม Wilcoxon signed-rank ประกอบ — ไม่ปล่อยผ่านเงียบ ๆ

### ตารางที่ 3 — คะแนนที่ระบบตรวจรายบุคคล

เต็ม 58 คะแนน เพราะระบบตรวจได้ 10 ตัวบ่งชี้ ส่วนข้อ 4.3 ความเรียบร้อย (2 คะแนน)
ต้องดูจากต้นฉบับลายมือ ครูจึงเป็นผู้ให้เอง ตัวเลขที่แสดงคือคะแนน**หลังครูปรับรายข้อแล้ว**
(ถ้ามีการปรับ จะมีป้าย “ปรับ N” กำกับ) พร้อม M, SD ท้ายตาราง

### ตารางที่ 4 — ผลการเปรียบเทียบแบบจับคู่ (คะแนนอัตโนมัติ)

คู่เดียวกับตารางที่ 2 คิดแยกกันคนละคู่เช่นเดิม บนสเกล 58 คะแนน

### ตารางที่ 5 — เทียบคะแนนอัตโนมัติกับคะแนนครูรายรอบ

เทียบบน**สเกลเดียวกัน** คือนับเฉพาะ 10 ข้อที่ระบบตรวจได้ (เต็ม 58)
โดยตัดข้อ 4.3 ออกจากคะแนนของครูด้วย มิฉะนั้นจะเป็นการเทียบคนละฐานคะแนน
รายงาน M, SD ของทั้งสองฝ่าย ผลต่าง (ระบบ − ครู) paired t-test และ Pearson r

> ค่า p ที่ **ไม่** มีนัยสำคัญในตารางนี้เป็นสัญญาณที่ดี — แปลว่าคะแนนของระบบ
> กับของครูไม่ต่างกันอย่างเป็นระบบ ส่วน r ยิ่งใกล้ 1 ยิ่งจัดลำดับนักเรียนได้ตรงกัน
> ไม่ว่าผลออกมาอย่างไร **การตัดสินผลยังใช้คะแนนของครูเท่านั้น**

---

## ขอบเขตข้อมูลและพารามิเตอร์ (GET)

| พารามิเตอร์ | ความหมาย |
|---|---|
| `group`     | กลุ่มการวิจัย · `all` = ทุกกลุ่ม (ปกติมาจากปุ่มเลือกกลุ่มบนแถบด้านบน) |
| `classroom` | ห้องเรียน · ไม่ระบุ = ทุกห้อง (เลือกจาก dropdown ในหน้า) |
| `export=csv`| ดาวน์โหลดทุกตารางเป็นไฟล์ CSV (UTF-8 BOM เปิดใน Excel ได้ทันที) |

ปุ่มเลือกกลุ่มบนแถบด้านบนเก็บค่าไว้ใน `localStorage` ร่วมกันทุกหน้าครู
หน้านี้คำนวณฝั่งเซิร์ฟเวอร์ จึงส่งค่ากลุ่มไปกับ URL ให้อัตโนมัติเมื่อเปิดครั้งแรก
และโหลดหน้าใหม่เมื่อครูกดเปลี่ยนกลุ่ม

---

## สถิติที่คำนวณให้

| สถิติ | วิธี |
|---|---|
| Mean, SD | ส่วนเบี่ยงเบนมาตรฐานของกลุ่มตัวอย่าง (หาร n−1) |
| Paired-samples t-test | ค่า p สองทางจากการแจกแจงที |
| Cohen's d<sub>z</sub> | ผลต่างเฉลี่ย ÷ SD ของผลต่าง พร้อมแปลผลตามเกณฑ์ Cohen (1988) |
| ช่วงความเชื่อมั่น 95% ของผลต่าง | คำนวณไว้ในชั้นข้อมูล (`ci_low` / `ci_high`) |
| Shapiro-Wilk | AS R94 (Royston, 1995) — อัลกอริทึมเดียวกับที่ SPSS และ R ใช้ |
| Pearson r | ใช้เทียบคะแนนอัตโนมัติกับคะแนนครูในตารางที่ 5 |

ทุกฟังก์ชันมาจาก `chapter45_stats.php` ชุดเดียวกับที่บทที่ 4-5 ใช้
ซึ่งตรวจสอบผลตรงกับ SciPy แล้ว ตัวเลขในหน้านี้จึงยกไปรายงานได้โดยไม่ต้องคำนวณซ้ำ

---

## ความสัมพันธ์กับหน้าอื่น

- **หน้าประเมินให้คะแนน** (`evaluation.php`) — ต้นทางของคะแนนทั้งหมดในหน้านี้
  รองรับพารามิเตอร์ `student` เพิ่มเข้ามาแล้ว เพื่อให้ลิงก์ *ให้คะแนน* กระโดดถึงคนที่ต้องตรวจได้ทันที
- **วิเคราะห์บทที่ 4-5** (`chapter45.php`) — ใช้ค่าเริ่มต้นเป็น *คะแนนเฉลี่ยของผู้ประเมินทุกคน*
  ถ้าต้องการให้บทที่ 4-5 ใช้คะแนนครูอย่างเดียวให้ตรงกับหน้านี้
  ตั้งค่า **คะแนนที่ใช้วิเคราะห์เชิงปริมาณ → ครูผู้สอน** ในกล่องข้อมูลประจำงานวิจัย
- **วิเคราะห์สถิติวิจัย** (`research_analysis.php`) — ค่า ICC ระหว่างผู้ประเมิน 3 คน
  ซึ่งเป็นคนละเรื่องกับหน้านี้ (หน้านี้ไม่คิด ICC เพราะใช้ผู้ประเมินคนเดียว)
