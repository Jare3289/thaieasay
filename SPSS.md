# ส่งออกข้อมูลดิบเข้า IBM SPSS Statistics

ระบบคำนวณสถิติของบทที่ 4 ให้ครบทุกตัวอยู่แล้ว (ดู `CHAPTER45.md`) แต่การส่งวิทยานิพนธ์
มักต้องแนบ **ผลจาก SPSS** เป็นหลักฐานว่าตัวเลขในตารางมาจากไหน หน้านี้จึงเตรียม
"ข้อมูลดิบ + ไฟล์คำสั่ง" ให้ครบชุด เปิด SPSS แล้วกด Run ได้ทันที ไม่ต้องพิมพ์คำสั่งเอง

เปิดได้ที่ **ตั้งค่าระบบ → แท็บ ส่งออกเข้า SPSS** (`settings.php#spss` · คุณครูเท่านั้น)
หรือกดปุ่ม *ส่งออกข้อมูลดิบเข้า SPSS* ในหน้าวิเคราะห์บทที่ 4-5

| ไฟล์ | หน้าที่ |
|---|---|
| `spss_export.php` | สร้างไฟล์ทั้งหมดจากข้อมูลจริงในระบบ (อ่านอย่างเดียว ไม่แก้ไขข้อมูลวิจัย) |
| `settings.php` แท็บ **ส่งออกเข้า SPSS** | หน้าจอกดส่งออกและคู่มือย่อบนเว็บ |

---

## ได้อะไรมาบ้าง

กดปุ่มเดียวได้ไฟล์ zip ที่มี 5 ไฟล์

| ไฟล์ | เนื้อหา |
|---|---|
| `spss_data.csv` | **ข้อมูลดิบรายบุคคล** 1 แถว = นักเรียน 1 คน — คะแนนดิบ 0-4 รายตัวบ่งชี้ทั้ง 11 ข้อ ของทั้ง 4 รอบ พร้อมจำนวนคำและจำนวนคำสะกดผิด |
| `spss_raters.csv` | **คะแนนแยกรายผู้ประเมิน** 1 แถว = นักเรียน 1 คนในรอบหนึ่ง ใช้คำนวณ ICC และค่าสหสัมพันธ์ระหว่างผู้ประเมิน |
| `spss_syntax.sps` | **ไฟล์คำสั่ง SPSS** ประกาศตัวแปร ป้ายชื่อภาษาไทย และสั่งวิเคราะห์ครบทุกตารางของบทที่ 4 |
| `spss_codebook.csv` | **พจนานุกรมตัวแปร** (ชื่อตัวแปร → ความหมาย → ค่าที่เป็นไปได้) แนบเป็นภาคผนวกได้ |
| `README_SPSS.txt` | คู่มือย่อฉบับพกพา สำหรับเปิดอ่านตอนนั่งอยู่หน้าเครื่องที่ลง SPSS |

ทดสอบกับ **SPSS Statistics รุ่น 27 ขึ้นไป รวมรุ่น 32** (คำสั่ง `/ES DISPLAY(TRUE)` ที่ให้ค่า Cohen's d
ในผลของ paired t-test มีตั้งแต่รุ่น 27 เป็นต้นไป)

---

## วิธีนำไปใช้ (5 ขั้น)

1. **ส่งออก** — เปิด **ตั้งค่าระบบ → ส่งออกเข้า SPSS** เลือกกลุ่มการวิจัยให้ตรงกับที่ใช้เขียนบทที่ 4
   แล้วกด *ดาวน์โหลดชุดข้อมูลทั้งชุด (.zip)*
2. **แตกไฟล์ทั้งหมดไว้ในโฟลเดอร์เดียวกัน** — ค่าเริ่มต้นคือ `C:\thaieasay_spss`
   (เปลี่ยนได้ในช่อง *โฟลเดอร์ที่จะเก็บไฟล์* ก่อนกดส่งออก ระบบจะเขียน path นั้นลงในไฟล์คำสั่งให้เลย)
3. **เปิดไฟล์คำสั่ง** — ใน SPSS เลือก **File → Open → Syntax…** แล้วเลือก `spss_syntax.sps`
   ถ้าเก็บไฟล์ไว้โฟลเดอร์อื่น ให้กด Ctrl+H แทนที่ path เดิมด้วยโฟลเดอร์จริง (มี 4 แห่ง ไฟล์บอกไว้ให้แล้ว)
4. **สั่งรัน** — กด **Ctrl+A** เลือกทั้งหมด แล้วกด **Ctrl+R** ผลทั้งหมดจะขึ้นในหน้าต่าง Output
5. **บันทึกผล** — บันทึก Output เป็น `.spv` หรือส่งออกเป็น Word/PDF เพื่อแนบเป็นหลักฐานของตัวเลขในบทที่ 4
   ไฟล์คำสั่งยังบันทึกชุดข้อมูลเป็น `thaieasay_main.sav` และ `thaieasay_raters.sav` ไว้ให้ใช้ต่อด้วย

---

## ผลจาก SPSS ตรงกับส่วนใดของวิทยานิพนธ์

| ตารางที่ SPSS พิมพ์ออกมา | ใช้เติมช่องใด |
|---|---|
| Descriptives | ตาราง 12 — M และ SD ก่อน/หลังเรียน ภาพรวมและ 4 ด้าน |
| Paired-Samples T Test | ตาราง 12 — ค่า t, df, p และขนาดอิทธิพล (Cohen's d) |
| Tests of Normality | ข้อตกลงเบื้องต้นก่อนใช้ t-test (Shapiro-Wilk ของคะแนนผลต่าง) |
| Wilcoxon Signed Ranks Test | ใช้แทน t-test เมื่อคะแนนผลต่างไม่เป็นการแจกแจงปกติ |
| Frequencies (`w1_def*` / `w2_def*`) | ตาราง 14 — จำนวนและร้อยละของผู้ปรากฏข้อบกพร่อง 11 ตัวบ่งชี้ |
| McNemar Test | ทดสอบว่าสัดส่วนผู้มีข้อบกพร่องระหว่างสองครั้งเปลี่ยนอย่างมีนัยสำคัญหรือไม่ |
| Intraclass Correlation Coefficient | ความเที่ยงระหว่างผู้ประเมิน ICC(3,1) และ ICC(3,k) |
| Correlations | ค่าสหสัมพันธ์ระหว่างผู้ประเมินรายคู่ (Pearson r) ในตาราง 12 |
| Reliability Statistics (Alpha) | ความสอดคล้องภายในของเกณฑ์ 11 ตัวบ่งชี้ (ข้อมูลประกอบ) |

---

## ตัวแปรในไฟล์ข้อมูลดิบ

ชื่อตัวแปรใช้อักษรอังกฤษเพื่อให้ SPSS ทุกรุ่นอ่านได้ ส่วนป้ายชื่อ (Variable Label) เป็นภาษาไทย
ไฟล์คำสั่งใส่ป้ายชื่อให้อัตโนมัติตั้งแต่ตอนนำเข้า จึงอ่านผลได้เป็นภาษาไทยทั้งหมด

| ตัวแปร | ความหมาย |
|---|---|
| `stu_code` · `stu_no` · `stu_name` · `classroom` · `grp` | รหัส · เลขนิรนามที่บทที่ 4 ใช้อ้าง · ชื่อ-สกุล · ห้อง · กลุ่มการวิจัย |
| `pre_i11` … `pre_i43` | คะแนนดิบ 0-4 รายตัวบ่งชี้ **ก่อนเรียน** (`i11` = ตัวบ่งชี้ 1.1 เรียงตามลำดับในเกณฑ์) |
| `post_i11` … `post_i43` | คะแนนดิบ 0-4 รายตัวบ่งชี้ **หลังเรียน** |
| `w1_i11` … `w1_i43` | คะแนนดิบ **ผลงานครั้งที่ 1** (รอบที่ตั้งไว้ในข้อมูลประจำงานวิจัย) |
| `w2_i11` … `w2_i43` | คะแนนดิบ **ผลงานครั้งที่ 2** |
| `pre_k` `post_k` `w1_k` `w2_k` | จำนวนผู้ประเมินที่คะแนนของรอบนั้นเฉลี่ยมาจาก |
| `pre_words` … `w2_words` | จำนวนคำในเรียงความแต่ละรอบ |
| `w1_miss` `w2_miss` | จำนวนคำสะกดผิดที่ตรวจพบ (นับชนิดคำที่ไม่ซ้ำกัน — เป็นค่าประมาณ) |

ตัวแปรที่ **ไฟล์คำสั่งสร้างขึ้นเองระหว่างรัน** (ไม่มีในไฟล์ CSV แต่มีในไฟล์ `.sav` ที่ได้)

| ตัวแปร | ความหมาย |
|---|---|
| `pre_d1`–`pre_d4` · `post_d1`–`post_d4` | คะแนนรวมรายองค์ประกอบหลังถ่วงน้ำหนัก (เต็ม 27/12/15/6) |
| `pre_tot` · `post_tot` | คะแนนรวมทั้งฉบับ (เต็ม 60) |
| `diff_tot` · `diff_d1`–`diff_d4` | ผลต่างหลังเรียน − ก่อนเรียน ใช้ทดสอบการแจกแจงปกติ |
| `w1_def11` … `w2_def43` | สถานะปรากฏข้อบกพร่องรายตัวบ่งชี้ (1 = ปรากฏ, 0 = ไม่ปรากฏ) |
| `base12` · `base14` | ฐานของตาราง 12 และตาราง 14 (มีคะแนนครบทั้งสองรอบ) |

**ทำไมไฟล์ CSV เก็บแค่คะแนนดิบ 0-4** — เพื่อให้ไฟล์คำสั่งคำนวณคะแนนถ่วงน้ำหนักและคะแนนรวม
ให้เห็นกันจะ ๆ ในบรรทัด `COMPUTE` ผู้อ่านวิทยานิพนธ์จึงตรวจสอบได้ว่าคะแนนเต็ม 60 ประกอบขึ้นมาอย่างไร
ตัวคูณน้ำหนักที่เขียนในไฟล์คำสั่งดึงมาจากเกณฑ์จริงในระบบ ไม่ได้พิมพ์ทิ้งไว้ตายตัว

---

## จุดที่ทำให้ตัวเลขตรงกับที่ระบบรายงาน

ไฟล์คำสั่งถูกสร้างจาก **ค่าที่ตั้งไว้จริงในระบบ** ทุกจุด ไม่ใช่ค่าตายตัวในโค้ด ได้แก่

- **ตัวคูณน้ำหนักของแต่ละตัวบ่งชี้** — ดึงจากเกณฑ์เดียวกับที่หน้าประเมินใช้
- **เกณฑ์นับว่า "ปรากฏข้อบกพร่อง"** — ดึงจากค่าที่ตั้งไว้แยกรายตัวบ่งชี้ในแท็บข้อมูลประจำงานวิจัย
- **คะแนนที่ใช้วิเคราะห์** — ค่าเฉลี่ยผู้ประเมินทุกคน / ครูอย่างเดียว / ผู้เชี่ยวชาญอย่างเดียว ตามที่ตั้งไว้
- **รอบงานที่นับเป็นผลงานครั้งที่ 1 และ 2** — ตามที่ตั้งไว้
- **ฐานการนับ** — `base12` และ `base14` บังคับให้นับเฉพาะนักเรียนที่มีคะแนนครบทั้งสองรอบ
  เหมือนที่ระบบทำ ทำให้ร้อยละของสองคอลัมน์เทียบกันได้จริง

นอกจากนี้ **ตัวเลขที่ระบบคำนวณไว้แล้วถูกแนบเป็นคอมเมนต์ในไฟล์คำสั่ง** (M, SD, t, p, d,
ค่า Shapiro-Wilk, ICC และตาราง 14 ทุกแถว) จึงเทียบกับผลที่ SPSS พิมพ์ออกมาได้ทีละบรรทัด
ถ้าไม่ตรงกัน สาเหตุที่พบบ่อยที่สุดคือ**เลือกกลุ่มการวิจัยตอนส่งออกคนละกลุ่มกับที่ดูอยู่บนหน้าจอ**

> **ความเที่ยงระหว่างผู้ประเมินคำนวณจากกลุ่มทดลองเสมอ** เช่นเดียวกับที่หน้าวิเคราะห์บทที่ 4-5 ทำ
> เพราะข้อมูลตรวจซ้ำโดยครูและผู้เชี่ยวชาญมีอยู่แค่กลุ่มนั้นกลุ่มเดียว ไม่ว่าจะเลือกส่งออกกลุ่มใดก็ตาม
> ถ้ายังไม่มีรอบใดที่มีผู้ประเมินตั้งแต่ 2 คนขึ้นไปให้คะแนนผลงานชุดเดียวกัน ส่วนนี้จะว่างไว้
> พร้อมคำอธิบายในไฟล์คำสั่ง แทนที่จะรันแล้วขึ้นข้อผิดพลาด

---

## ข้อควรระวัง

- **ลบคอลัมน์ `stu_name` ก่อนแนบชุดข้อมูลเป็นภาคผนวกเสมอ** — บทที่ 4 อ้างถึงนักเรียน
  ด้วยเลขนิรนามในคอลัมน์ `stu_no` เท่านั้น
- **อย่าเปิดไฟล์ CSV ใน Excel แล้วกดบันทึกทับ** — Excel มักบันทึกกลับเป็นรหัสอักขระอื่น
  ทำให้ SPSS อ่านภาษาไทยไม่ออก ถ้าจะดูเนื้อหาให้เปิดดูเฉย ๆ แล้วปิดโดยไม่บันทึก
- **จำนวนคำสะกดผิดเป็นค่าประมาณ** — นับด้วยพจนานุกรมอัตโนมัติ คำวิสามานยนามอาจถูกนับเกินจริง
  ควรสุ่มตรวจยืนยันก่อนรายงานเป็นตัวเลขในวิทยานิพนธ์
- **ตัวบ่งชี้ 4.3 ความเรียบร้อย** ประเมินจากต้นฉบับลายมือ ระบบส่งออกได้เฉพาะคะแนนที่คุณครูกรอกไว้
- ถ้าเปลี่ยนค่าในแท็บ **ข้อมูลประจำงานวิจัย** (เช่นเกณฑ์นับข้อบกพร่อง หรือรอบงานที่ใช้)
  ต้อง**ส่งออกใหม่**ทั้งชุด เพราะไฟล์คำสั่งฝังค่าเดิมไว้ตั้งแต่ตอนสร้าง
