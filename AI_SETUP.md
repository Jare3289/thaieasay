# คู่มือเปิดใช้งาน "ผู้ช่วย AI ตรวจเรียงความ"

ระบบนี้ให้ AI อ่านเรียงความของนักเรียน แล้วบอก **จุดแข็ง / จุดที่ควรปรับปรุง / วิธีแก้ /
คะแนนโดยประมาณตามเกณฑ์ของคุณครู** โดยที่ทั้งครูและนักเรียนเห็นผลตรวจได้

> **ข้อควรรู้ก่อนเริ่ม**
> คะแนนจาก AI เป็น **ข้อเสนอแนะเพื่อพัฒนางานเขียน ไม่ใช่คะแนนจริง**
> ระบบเก็บไว้คนละตารางกับคะแนนของครู/เพื่อน/ตนเอง จึง **ไม่กระทบข้อมูลงานวิจัย** ที่เก็บมาแล้ว

---

## ตอบคำถามที่ถามบ่อยที่สุด: มี AI ให้ใช้ฟรีไหม?

**มีค่ะ** ผู้ให้บริการหลายเจ้ามี "ชั้นใช้งานฟรี" (free tier) ที่พอสำหรับห้องเรียนขนาด 40 คน
เพียงสมัครและขอ **API key** (รหัสสำหรับให้เว็บเราเรียกใช้ AI) มาใส่ในระบบครั้งเดียว

| ผู้ให้บริการ | เหมาะกับ | โควตาฟรีโดยประมาณ | ขอคีย์ที่ |
|---|---|---|---|
| **Google Gemini (AI Studio)** ⭐ แนะนำ | ภาษาไทยดี เร็ว ใช้ง่าย | หลายสิบครั้ง/วัน โดยไม่ต้องผูกบัตร | https://aistudio.google.com/apikey |
| **OpenTyphoon** | โมเดลที่ฝึกกับภาษาไทยโดยเฉพาะ | มีชั้นทดลองใช้ฟรี | https://opentyphoon.ai/ |
| **OpenRouter** | เลือกโมเดลได้หลายตัว | โมเดลที่ลงท้าย `:free` ใช้ฟรี | https://openrouter.ai/keys |
| **Groq** | ตอบเร็วมาก | มีโควตาฟรีต่อวัน | https://console.groq.com/keys |

> โควตาฟรีของแต่ละเจ้าเปลี่ยนแปลงได้ตลอด โปรดดูเงื่อนไขล่าสุดในหน้าเว็บของผู้ให้บริการนั้น ๆ
> ระบบนี้ **ไม่ผูกกับเจ้าใดเจ้าหนึ่ง** — เปลี่ยนผู้ให้บริการได้ทีหลังโดยไม่ต้องแก้โค้ด
>
> **ชื่อโมเดลก็เปลี่ยนตามเวลาเช่นกัน** (ผู้ให้บริการทยอยเลิกใช้รุ่นเก่า) จึงแนะนำให้
> **เว้นช่อง "ชื่อโมเดล" ว่างไว้** ระบบจะใช้ค่าเริ่มต้นที่กำหนดไว้ในโค้ดให้เอง
> ถ้าวันหนึ่งขึ้นว่า *ไม่พบโมเดลที่ตั้งค่าไว้* ข้อความ error จะบอกชื่อรุ่นใหม่มาให้ด้วย

**แนะนำสำหรับโรงเรียน: เริ่มที่ Google Gemini** เพราะสมัครด้วยบัญชี Google ที่มีอยู่แล้ว
ไม่ต้องกรอกบัตรเครดิต และรองรับภาษาไทยได้ดี

---

## ขั้นที่ 1 — ขอ API key (ทำครั้งเดียว)

ตัวอย่างของ **Google Gemini**:

1. เปิด https://aistudio.google.com/apikey แล้วลงชื่อเข้าใช้ด้วยบัญชี Google
2. กดปุ่ม **Create API key** (สร้างคีย์)
3. จะได้ข้อความยาว ๆ ขึ้นต้นด้วย `AIza...` → **คัดลอกเก็บไว้**
   (คีย์นี้เหมือนรหัสผ่าน อย่าส่งต่อให้ผู้อื่นและอย่าโพสต์ในที่สาธารณะ)

---

## ขั้นที่ 2 — ใส่คีย์เข้าระบบ

เลือก **วิธีใดวิธีหนึ่ง** ก็พอ

### วิธี ก — กรอกในหน้าเว็บ (ง่ายที่สุด แนะนำสำหรับคุณครู)

1. เข้าเว็บด้วยบัญชี **ครู** → เมนู **ผู้ช่วย AI ตรวจงาน**
2. กดปุ่ม **เปิด/ปิดการตั้งค่า** ในการ์ด "ตั้งค่าผู้ช่วย AI"
3. เลือก **ผู้ให้บริการ AI** = `Google Gemini (AI Studio)`
4. วาง API key ลงในช่อง **API key**
5. เปิดสวิตช์ **เปิดใช้งานผู้ช่วย AI ทั้งระบบ**
6. กด **บันทึกการตั้งค่า** — แถบด้านบนควรขึ้นว่า *ผู้ช่วย AI พร้อมใช้งาน*

### วิธี ข — วางไฟล์บนเซิร์ฟเวอร์ (ปลอดภัยกว่า คีย์ไม่ถูกเก็บในฐานข้อมูล)

1. คัดลอกไฟล์ `ai_secrets.sample.php` เป็น `ai_secrets.php` (โฟลเดอร์เดียวกับ `ai_config.php`)
2. แก้ค่าในไฟล์:
   ```php
   $ai_provider = 'gemini';
   $ai_model    = 'gemini-3.6-flash';
   $ai_api_key  = 'AIza...คีย์จริงของคุณ...';
   $ai_base_url = '';
   ```
3. บันทึกไฟล์ — ระบบจะใช้คีย์จากไฟล์นี้ทันที (สำคัญกว่าค่าที่กรอกในเว็บเสมอ)

> ไฟล์ `ai_secrets.php` อยู่ใน `.gitignore` แล้ว จึงไม่ถูกอัปขึ้น git

---

## ใครสั่งตรวจได้บ้าง

**คุณครูเป็นผู้สั่งตรวจเพียงผู้เดียว** นักเรียนกดให้ AI ตรวจเองไม่ได้ เห็นได้เฉพาะผลที่
คุณครูตรวจให้แล้วเท่านั้น — ออกแบบไว้แบบนี้เพื่อให้คุณครูควบคุมได้ว่าจะให้ AI ตรวจรอบไหน
เมื่อไร และไม่ให้โควตาฟรีถูกใช้หมดโดยไม่รู้ตัว

---

## วิธีใช้งาน

### 🔥 ตรวจทั้งรอบรวดเดียว (วิธีหลักที่แนะนำ)

หน้า **ผู้ช่วย AI ตรวจงาน** → การ์ด **ตรวจทั้งรอบรวดเดียว**

1. เลือก **รอบงานที่จะตรวจ** (เช่น ก่อนเรียน / ภาระงานหน่วยที่ 1 ร่างที่ 2)
2. เลือก **ห้องเรียน** ถ้าต้องการตรวจทีละห้อง (ไม่เลือก = ทุกห้อง)
3. ติ๊ก **ข้ามฉบับที่เคยตรวจแล้ว** ไว้ (ค่าเริ่มต้น) เพื่อไม่ให้เปลืองโควตาตรวจซ้ำ
4. กด **เริ่มตรวจทั้งรอบ**

ระบบจะไล่ตรวจทีละฉบับจนครบ พร้อมแถบความคืบหน้าและรายการผลแบบสด ๆ
กด **หยุด** ได้ตลอด (จะหยุดหลังตรวจฉบับที่กำลังทำอยู่เสร็จ)

> ⏱ ใช้เวลาราว **25-30 วินาทีต่อฉบับ** — ทั้งห้อง 39 คนใช้เวลาประมาณ 18-20 นาที
> **ต้องเปิดหน้านี้ค้างไว้จนกว่าจะเสร็จ** ถ้าปิดไปกลางคัน ฉบับที่ตรวจไปแล้วยังอยู่ครบ
> เปิดใหม่แล้วกด **"ตรวจต่อจากที่ค้างไว้"** ได้เลย (ดูหัวข้อถัดไป)

### ⏸ ตรวจต่อจากที่ค้างไว้

ถ้าการตรวจเป็นชุดหยุดกลางคัน — **โควตาวันนั้นหมด**, กด **หยุด**, เน็ตหลุด หรือเผลอปิดหน้าเว็บ —
ระบบจะ **จำรายการที่ยังไม่ได้ตรวจของชุดนั้นไว้** (เก็บไว้ในเครื่องของครู เครื่องเดิม-เบราว์เซอร์เดิม)

เปิดหน้า **ผู้ช่วย AI ตรวจงาน** ระบบจะ **กางการ์ด "ตรวจทั้งรอบรวดเดียว" ให้เอง**
พร้อมป้ายสีเหลือง *ค้าง N* ที่ปุ่มเครื่องมือ ในการ์ดจะเห็นแถบสีเหลือง
*"มีคิวตรวจค้างอยู่จากครั้งที่แล้ว"* บอกว่าเป็นชุดไหน ค้างเพราะอะไร ตรวจไปแล้วกี่ฉบับ
และเหลือรอบไหนอีกกี่ฉบับ — กด **"ตรวจต่อจากที่ค้างไว้"** ระบบจะไล่ตรวจ
**เฉพาะฉบับที่ยังไม่ได้ตรวจในชุดเดิม** ต่อจากจุดที่ค้าง ไม่เริ่มนับหนึ่งใหม่และไม่ตรวจซ้ำให้เปลืองโควตา

- ใช้ได้กับทุกชุด: ตรวจทั้งรอบ · ตรวจใหม่ทุกรอบรวดเดียว · ตรวจซ้ำเฉพาะที่ไม่สำเร็จ · ตรวจใหม่ทั้งคิว
- ถ้าตรวจต่อแล้วโควตาหมดอีก ระบบก็จะจำคิวที่เหลือไว้ให้อีกครั้ง กดต่อได้เรื่อย ๆ จนครบ
- ไม่อยากตรวจต่อแล้ว กด **"ล้างคิวที่ค้าง"** ได้ (ไม่มีผลกับผลตรวจที่บันทึกไว้แล้ว)
- คิวที่ค้างนานเกิน 30 วันระบบจะทิ้งเอง

ก่อนกด ระบบจะบอกให้ทราบว่า:
- ส่งแล้วกี่ฉบับ · ตรวจไปแล้วกี่ฉบับ · จะตรวจรอบนี้กี่ฉบับ
- มีกี่ฉบับที่สั้นเกินเกณฑ์จนระบบไม่ส่งให้ AI ตรวจ
- โควตาที่เหลือวันนี้พอหรือไม่

### 🔁 ตรวจใหม่ทุกรอบรวดเดียว (ปุ่มสีแดงในการ์ดเดียวกัน)

ใช้เมื่อ **กติกาการตรวจเปลี่ยน** แล้วอยากให้ผลตรวจของทั้งชั้นทุกรอบมาจากกติกาชุดเดียวกัน
(เช่น หลังปรับคำสั่งเรื่องชนิดงานเขียนหรือการเทียบร่าง) กดปุ่มเดียวจบ ไม่ต้องไล่ทีละรอบ

- ไล่ตรวจ **ทุกรอบตามลำดับการเรียน**: ก่อนเรียน → D1.1 → D1.2 → D2.1 → D2.2 → หลังเรียน
  ลำดับนี้สำคัญ เพราะร่างหลังต้องเทียบกับฉบับตั้งต้น — ตรวจฉบับตั้งต้นก่อนเสมอ ผลเทียบร่างจึงครบทุกคู่
- **ตรวจซ้ำทุกฉบับ** รวมฉบับที่เคยตรวจแล้ว (ไม่สนใจช่อง "ข้ามฉบับที่เคยตรวจแล้ว")
  และ **คะแนนที่ครูปรับไว้รายข้อจะถูกล้าง** เพราะเป็นการตรวจใหม่ทั้งฉบับ
- เลือกห้องเรียนก่อนกดได้ (ใช้ช่อง "ห้องเรียน" ช่องเดียวกัน) · กด **หยุด** ได้ตลอด
- กล่องยืนยันจะบอกจำนวนฉบับ เวลาที่ใช้โดยประมาณ และเตือนถ้าโควตาวันนี้ไม่พอ

> ⏱ ทั้งห้อง 39 คน × 6 รอบ ≈ 234 ฉบับ ใช้เวลาราว 1.5-2 ชั่วโมง และกินโควตา 234 ครั้ง
> (เพดานเริ่มต้นคือ 400 ครั้ง/วัน ปรับได้ในการ์ด "ตั้งค่าผู้ช่วย AI") — ควรกดตอนที่ไม่ต้อง
> ใช้เครื่องทำอย่างอื่น และเปิดหน้าค้างไว้ · ถ้าโควตาหมดกลางคัน กด **"ตรวจต่อจากที่ค้างไว้"**
> ในวันถัดไปได้เลย ระบบจำคิวที่เหลือไว้ให้

### ตรวจทีละฉบับ

เลือกนักเรียนคนเดียวจากช่องด้านบน — ระบบจะขึ้น **การ์ดคะแนนของทุกรอบงานพร้อมกัน**
(ก่อนเรียน / หน่วย 1 ร่าง 1-2 / หน่วย 2 ร่าง 1-2 / หลังเรียน) ไม่ต้องเลือกรอบงานทีละรอบ
แต่ละการ์ดบอกคะแนนรวมเต็ม 60 ระดับคุณภาพ และป้ายเตือน "รอคะแนนครู" / "รอตรวจใหม่"

- **คลิกที่การ์ด** → เปิดรายละเอียดการให้คะแนนรายเกณฑ์และข้อเสนอแนะของฉบับนั้นด้านล่าง
  (คลิกการ์ดเดิมซ้ำ หรือกด "ปิดรายละเอียด" เพื่อยุบกลับ)
- **ปุ่ม "ให้ AI ตรวจ" / "ตรวจใหม่" บนการ์ด** → สั่งตรวจเฉพาะรอบนั้น
  (การ์ดของรอบที่ยังไม่ได้เขียน หรือเรียงความสั้นกว่าเกณฑ์ จะไม่มีปุ่มนี้)

ด้านล่างสุดมีตาราง **ภาพรวมผลตรวจ AI ทั้งชั้น** คลิกช่องคะแนนใดก็ได้เพื่อกระโดดไปเปิด
รายละเอียดของฉบับนั้น และลบผลที่ไม่เหมาะสมได้ด้วยปุ่ม **ลบผลตรวจนี้**

ในหน้า **เรียงความนักเรียน** ปุ่มหุ่นยนต์ 🤖 ในแต่ละช่องพาไปยังผลตรวจของฉบับนั้นโดยตรง

### สำหรับนักเรียน (ดูอย่างเดียว)

- **หน้า "บันทึกเรียงความ"** — เมื่อคุณครูตรวจรอบนั้นแล้ว ข้อเสนอแนะจะขึ้นในกล่องสีม่วง
  ใต้ช่องเขียน (ถ้ายังไม่ตรวจ กล่องนี้จะไม่แสดง)
- **หน้า "ผู้ช่วย AI ตรวจงาน"** — เห็นการ์ดคะแนนของตนเองครบทุกรอบงานทันทีที่เปิดหน้า
  ไม่ต้องเลือกรอบงาน คลิกการ์ดใบไหนก็ได้เพื่อดูรายละเอียดการให้คะแนนและข้อมูลย้อนกลับของรอบนั้น

### สำหรับผู้เชี่ยวชาญ
- ดูผลตรวจของ AI ได้อย่างเดียว (สั่งตรวจไม่ได้ ตั้งค่าไม่ได้)

---

## แก้ไขต้นฉบับแล้วเข้าคิวตรวจใหม่อัตโนมัติ

ถ้านักเรียน **แก้ไขเรียงความที่ AI เคยตรวจไปแล้ว** ผลตรวจเดิมจะกลายเป็นของฉบับก่อนแก้
ระบบจึงจัดฉบับนั้นเข้า **คิวรอตรวจใหม่** ให้เองทันทีที่นักเรียนกดบันทึก

ระบบเทียบด้วย "ลายนิ้วมือ" ของเนื้อหา (ส่วนนำ + เนื้อเรื่องทุกย่อหน้า + สรุป) ไม่ใช่เวลาบันทึก
ดังนั้น **กดบันทึกซ้ำโดยไม่แก้อะไรเลยจะไม่เข้าคิว** และถ้านักเรียนแก้แล้วย้อนกลับมาเหมือนเดิมเป๊ะ
ระบบจะถอดออกจากคิวให้เอง

เห็นได้ที่ไหนบ้าง:

| ที่ | เห็นอะไร |
|---|---|
| หน้า **ผู้ช่วย AI** (ครู/ผู้เชี่ยวชาญ) | การ์ด **คิวรอตรวจใหม่** ด้านบนสุด บอกจำนวนและรายชื่อ พร้อมปุ่ม **ให้ AI ตรวจใหม่ทั้งคิว** (ครูเท่านั้น) |
| ผลตรวจของฉบับนั้น | แถบเหลือง "ต้นฉบับถูกแก้ไขหลังจาก AI ตรวจ" + ปุ่ม **ให้ AI ตรวจใหม่ตอนนี้** |
| ตาราง **ภาพรวมผลตรวจ AI ทั้งชั้น** | สัญลักษณ์วนซ้ำในช่องคะแนน และตัวกรอง **เฉพาะที่รอตรวจใหม่** |
| **ตรวจทั้งรอบรวดเดียว** | ฉบับที่รอตรวจใหม่จะถูกตรวจให้ แม้ติ๊ก "ข้ามฉบับที่เคยตรวจแล้ว" ไว้ |
| หน้า **บันทึกเรียงความ** (นักเรียน) | ข้อความแจ้งตอนบันทึก และแถบ "รอคุณครูสั่งให้ AI ตรวจใหม่" ในกล่องผลตรวจ |

คะแนนข้อ 4.3 ที่คุณครูกรอกไว้เองจะไม่หายไปเมื่อตรวจใหม่

---

## เทียบร่างตามคู่ที่คุณครูกำหนด (D1.2 ↔ D1.1 · D2.2 ↔ D2.1 · หลังเรียน ↔ ก่อนเรียน)

ระบบเทียบผลตรวจ **ตามคู่ที่คุณครูกำหนดเท่านั้น** คือร่างที่ 2 กับร่างที่ 1 ของหน่วยเดียวกัน
และหลังเรียนกับก่อนเรียน — ไม่เทียบข้ามหน่วย (D2.1 จึงไม่ถูกนำไปเทียบกับ D1.2)
และ**ไม่มีการเทียบผลตรวจครั้งนี้กับการตรวจครั้งก่อนของฉบับเดียวกัน**
ถ้าคุณครูสั่งตรวจฉบับเดิมซ้ำ ผลตรวจใหม่จะทับของเดิมและเทียบกับฉบับตั้งต้นตามคู่เหมือนเดิมทุกครั้ง

| คู่ที่เทียบ | ฉบับตั้งต้น |
|---|---|
| D1.2 (ภาระงานหน่วยที่ 1 · ร่างที่ 2) | D1.1 (ร่างที่ 1 ของหน่วยเดียวกัน) |
| D2.2 (ภาระงานหน่วยที่ 2 · ร่างที่ 2) | D2.1 (ร่างที่ 1 ของหน่วยเดียวกัน) |
| หลังเรียน (Post-test) | ก่อนเรียน (Pre-test) |

**ตอนสั่งตรวจ** ระบบทำ 2 อย่าง

*ก) เทียบข้อความเองก่อน* — แยกเรียงความทั้งสองฉบับเป็นส่วน ๆ (คำนำ · เนื้อเรื่องทีละย่อหน้า · สรุป)
แล้วเทียบทีละส่วนว่า **แก้ไข / เพิ่มใหม่ / ถูกตัดออก / ไม่ได้แก้เลย** พร้อมดึงข้อความที่เพิ่มเข้ามา
และข้อความที่ถูกตัดออกมาให้เห็น ส่วนนี้ระบบคำนวณเอง **ไม่ได้ถาม AI** จึงยืนยันได้แน่นอนว่าส่วนที่ขึ้นว่า
"ไม่ได้แก้" คือเหมือนเดิมทุกตัวอักษรจริง

*ข) ส่งให้ AI พร้อมข้อมูลครบ* — แนบ *ตัวเรียงความของฉบับตั้งต้นทั้งฉบับ* คะแนนรายข้อ จุดที่เคยแจ้งให้แก้
**และผลเทียบข้อความจากข้อ ก)** ไปด้วย แล้วกำกับ AI ว่า

1. **ยึดคะแนนรายข้อของฉบับตั้งต้นเป็น "จุดตั้งต้น"** ของทุกข้อ แล้วไล่หาหลักฐานว่าข้อนั้นดีขึ้นตรงไหนก่อนเสมอ
   โดยเฉพาะจุดที่เคยแจ้งให้แก้ในฉบับตั้งต้น — จุดไหนที่นักเรียนแก้มาแล้วจริง (แม้แก้ได้บางส่วนแต่ดีขึ้นชัดเจน)
   ให้**ขยับคะแนนข้อนั้นขึ้น 1 ระดับ** และข้อที่ก้ำกึ่งระหว่างสองระดับ ให้เลือก**ระดับที่สูงกว่า**
   (เฉพาะข้อที่ส่วนเกี่ยวข้องถูกแก้จริงเท่านั้น)
1.1 **ขยับทีละ 1 ระดับเป็นปกติ** — การแก้ร่างหนึ่งครั้งคือการพัฒนาหนึ่งขั้น จะขึ้น 2 ระดับในข้อเดียวได้
   ต่อเมื่อ (ก) ส่วนนั้นถูกเขียนใหม่ยกส่วนจริง (ข) ยกข้อความทั้งสองฉบับมาวางคู่กันได้ชัด
   (ค) เข้าเกณฑ์ของระดับใหม่ครบตามคำบรรยายแบบไม่มีข้อกังขา — ไม่ครบสามข้อนี้ ให้ขึ้นแค่ 1 ระดับ
1.2 **ส่วนที่ "เหมือนฉบับตั้งต้นทุกตัวอักษร" ต้องได้คะแนนเท่าเดิมเป๊ะ — ทั้งขึ้นและลง**
   และคะแนนที่ให้ต้องเข้าเกณฑ์ของระดับนั้นจริงตามคำบรรยาย (นับจากงานจริง) ห้ามให้สูงขึ้น
   เพียงเพราะ "แก้มาแล้ว" โดยเฉพาะระดับ 4 ที่แปลว่าไม่มีข้อบกพร่องเลย
1.3 ภาพรวมต้องสมเหตุสมผล — แก้ไม่กี่จุด คะแนนก็ควรขยับเฉพาะข้อที่เกี่ยวกับจุดนั้น **ไม่ใช่ขึ้นยกแผงทุกข้อ**
2. **ห้ามให้คะแนนข้อใดต่ำกว่าฉบับตั้งต้น** เว้นแต่ยก*ข้อความจริง*มายืนยันได้ว่าร่างหลังทำข้อนั้นได้แย่ลงจริง
   (ตัดเนื้อหาดี ๆ ออก · เพิ่มเรื่องนอกหัวข้อ · สะกดผิดมากขึ้น) ถ้ายกหลักฐานไม่ได้ ให้คงคะแนนไว้เท่าเดิม
   ส่วนที่ระบบระบุว่า **"เหมือนฉบับตั้งต้นทุกตัวอักษร" ต้องได้คะแนนเท่าเดิมเสมอ** — คะแนนที่ตกลง
   เพราะรอบนี้ผู้ตรวจเข้มงวดกว่ารอบก่อน ถือเป็นความผิดพลาดของการตรวจ ไม่ใช่ผลงานของนักเรียน
   ก่อนตอบ AI ต้องไล่ตรวจทานคะแนนทุกข้อเทียบกับฉบับตั้งต้นอีกรอบตามกติกาข้อนี้
3. แต่ก็ **ห้ามบวกคะแนนลอย ๆ** เพราะเห็นว่าตั้งใจแก้ หรือเพราะคาดว่าร่างหลังต้องดีกว่า —
   ทุกคะแนนที่ขยับขึ้นต้องชี้ได้ว่ามาจากข้อความส่วนไหนของร่างหลัง
4. เทียบรายข้อโดยยก **ข้อความจริงของทั้งสองฉบับ** มาวางคู่กัน (ก่อน → หลัง)
5. **ทุกข้อต้องเขียนว่า "แก้อะไรไป แก้อย่างไร อยู่ส่วนไหนของเรียงความ"** ให้เป็นรูปธรรม เช่น
   *ตัดย่อหน้าที่ 3 ที่พูดถึงเรื่องอื่นออก* / *เพิ่มตัวอย่างสองเรื่องในย่อหน้าที่ 2* /
   *เปลี่ยนคำเชื่อม "แล้วก็" เป็น "นอกจากนี้"* — และบอกต่อว่าการแก้นั้นทำให้ข้อนั้นดีขึ้นหรือไม่อย่างไร
   ข้อที่ไม่มีอะไรเปลี่ยนก็ต้องบอกว่ายังไม่ได้แก้
6. ข้อที่คะแนนเท่าเดิม ต้องบอกว่าเพราะยังไม่ได้แก้ หรือแก้แล้วแต่ยังไม่ถึงระดับถัดไป และต้องทำอะไรจึงจะขยับขึ้น
7. ถ้าฉบับใหม่แทบไม่ต่างจากฉบับตั้งต้น ให้พูดตรง ๆ ว่าไม่ต่าง คะแนนคงเท่าเดิม (เท่าเดิมได้ แต่ห้ามต่ำลงถ้าไม่มีหลักฐาน)
8. **ห้ามบอกว่านักเรียนแก้ส่วนที่ระบบระบุว่าไม่ได้แก้** — กันไม่ให้ AI ชมงานที่ยังไม่ได้แตะ

> คู่ **หลังเรียน ↔ ก่อนเรียน** เป็นคนละหัวข้อ จึงไม่เทียบตัวข้อความ แต่ใช้หลักเดียวกันเรื่องมาตรฐานคะแนน:
> ต้องมองหาทักษะที่ทำได้ดีขึ้นก่อน ข้อก้ำกึ่งให้ระดับที่สูงกว่า และจะให้ต่ำกว่าฉบับก่อนเรียนได้
> ต่อเมื่อชี้หลักฐานได้ว่าทักษะข้อนั้นถดถอยจริง
>
> การ **"สั่งให้ AI ตรวจใหม่เฉพาะข้อ"** บนร่างหลัง ก็แนบคะแนนเดิมของข้อนั้นจากฉบับตั้งต้นไปด้วย
> และใช้กติกาชุดเดียวกัน คะแนนของข้อนั้นจึงไม่ตกต่ำกว่าร่างแรกโดยไม่มีเหตุผล

**ตอนแสดงผล** ในผลตรวจของ D1.2 / D2.2 / หลังเรียน จะมีกล่อง **"เทียบกับ ..."** อยู่บนสุด
สีของกล่องบอกผลทันที: เขียว = คะแนนดีขึ้น · เหลือง = เท่าเดิม · แดง = ถอยลง ภายในกล่องมี

| ส่วน | บอกอะไร |
|---|---|
| คำตัดสินบรรทัดเดียว | คะแนนดีขึ้นจากฉบับตั้งต้นกี่คะแนน หรือยังไม่ดีขึ้น |
| ธงเตือน | ขึ้นเมื่อผลผิดจากที่ควรเป็น — **ข้อความเหมือนฉบับตั้งต้นทุกตัวอักษร** (นักเรียนยังไม่ได้แก้งาน), **คะแนนรายข้อเท่ากันทุกข้อ**, คะแนนรวมยังไม่ดีขึ้น, หรือ **คะแนนกระโดดขึ้นตั้งแต่ 2 ระดับในข้อเดียว** (บอกด้วยว่าเป็นข้อไหน จากเท่าไรเป็นเท่าไร — ให้ครูตรวจทานว่า AI ให้เกินจริงหรือไม่ แล้วปรับคะแนนข้อนั้นเองหรือสั่งตรวจข้อนั้นใหม่ได้) |
| แถบสรุป | คะแนน AI ฉบับตั้งต้น → ฉบับนี้, ระดับคุณภาพ, ดีขึ้น/ลดลง/เท่าเดิมกี่ข้อ, จำนวนคำ |
| **สิ่งที่เปลี่ยนไปจากฉบับตั้งต้น** | รายการทีละส่วนของเรียงความ (คำนำ · ย่อหน้าที่ 1, 2, 3 … · สรุป) ว่าส่วนไหน **แก้ไข / เพิ่มใหม่ / ตัดออก / ไม่ได้แก้** จำนวนคำก่อน → หลัง พร้อม**ข้อความที่เพิ่มเข้ามา**และ**ข้อความที่ถูกตัดออก** |
| ตารางเทียบรายข้อ | **แต่ละแถวอ่านจบได้ในตัวเอง** — ดูรายละเอียดด้านล่าง |

### ตารางเทียบรายเกณฑ์ — แต่ละแถวตอบ 4 เรื่องเรียงกัน

ทุกแถวเขียนให้อ่านจบได้ในตัวเอง ไม่ต้องเหลือบไปดูคอลัมน์อื่นประกอบ

1. **ข้อนี้คือเกณฑ์อะไร** พร้อมป้ายบอกทันทีว่าดีขึ้น / เท่าเดิม / แย่ลง
2. **คะแนนเปลี่ยนจากเท่าไรเป็นเท่าไร** เขียนเป็นประโยค เช่น *"จาก 6 คะแนน เป็น 9 คะแนน จากเต็ม 12 คะแนน · ยังเสียอยู่ 3 คะแนน"*
   (ข้อที่ไม่ขยับจะขึ้นว่า *"ได้ 3 คะแนนเท่าเดิมทั้งสองฉบับ"*)
3. **อะไรเพิ่มเข้ามา / อะไรหายไป** ยกข้อความจริงมาให้เห็น พร้อมบอกว่าอยู่ส่วนไหนของเรียงความ
4. **ทำไมคะแนนถึงสูงขึ้น / ต่ำลง / เท่าเดิม** โยงกับสิ่งที่เพิ่มและหายไปในข้อ 3 ตามเกณฑ์ข้อนั้น
   และถ้ายังไม่ดีขึ้น จะมีบรรทัด *"ทำอย่างไรให้ขยับขึ้นอีก"* ต่อท้าย

ตัวอย่างหนึ่งแถว

> **1.1 ความตรงประเด็น** `ดีขึ้น`
> จาก **6** คะแนน เป็น **9** คะแนน จากเต็ม 12 คะแนน · ยังเสียอยู่ 3 คะแนน
> `เพิ่มเข้ามา` ย่อหน้าที่ 2 เพิ่มตัวอย่างสองเรื่องที่โยงกับหัวข้อโดยตรง
> `หายไป / ของเดิม` ตัดย่อหน้าที่ 3 เดิมที่เล่าเรื่องไปเตะบอลกับเพื่อน ซึ่งไม่เกี่ยวกับหัวข้อ ออกทั้งย่อหน้า
> **ทำไมคะแนนถึงสูงขึ้น:** เดิมมีประเด็นนอกขอบเขต 1 ประเด็นจึงได้ระดับ 2 พอตัดย่อหน้านั้นออกและเพิ่มตัวอย่างที่ตรงหัวข้อ ทุกย่อหน้าจึงสัมพันธ์กับหัวข้อทั้งหมด ได้ระดับ 3

AI ถูกกำกับไว้ว่า **ห้ามเขียนเหตุผลลอย ๆ ว่า "เขียนดีขึ้น"** — ต้องอ้างสิ่งที่เพิ่มหรือหายไปจริง ๆ
ที่ระบุไว้ในแถวเดียวกัน ส่วนคะแนนที่แสดงเป็นคะแนนที่ AI ให้จริงทั้งสองฉบับ ระบบคำนวณส่วนต่างเอง

จุดอื่น ๆ ที่เห็นผลเทียบร่างได้:

- **การ์ดคะแนนรายรอบงาน** — บรรทัด "เทียบ D1.1: +7 คะแนน · ดีขึ้น"
- **ตารางภาพรวมทั้งชั้น** — สัญลักษณ์ลูกศรเขียว (ดีขึ้นกว่าฉบับตั้งต้น) / วงกลมเตือนสีส้ม (ยังไม่ดีขึ้น)
- **แท็บภาพรวมผลตรวจ AI ทั้งชั้น** — การ์ด "ร่างหลังดีขึ้นจริง" บอกเป็นเปอร์เซ็นต์ของทั้งชั้น
- **หน้าสรุปภาพรวมผลงานเขียน** — การ์ดคู่เทียบทั้ง 3 คู่เรียงกัน พร้อมบทสรุปว่าผ่านกี่คู่

หมายเหตุ:

- ถ้าฉบับตั้งต้นยังไม่ถูก AI ตรวจ ระบบจะเทียบคะแนนรายข้อให้ไม่ได้ — ตอนกดตรวจจะมีกล่องถามยืนยันก่อน และในผลตรวจจะขึ้นว่า "ยังเทียบกับ ... ไม่ได้"
- ผลตรวจที่บันทึกไว้**ก่อน**มีฟีเจอร์นี้ยังเห็นผลเทียบได้ — ระบบเทียบข้อความให้สด ๆ ตอนเปิดดู
  จึงเห็น "สิ่งที่เปลี่ยนไปจากฉบับตั้งต้น" และคะแนนที่ขยับรายข้อครบ แต่จะยังไม่มีคำอธิบายรายข้อจาก AI
  (อะไรเพิ่ม อะไรหาย เพราะอะไร) ตารางจะขึ้นหมายเหตุบอกไว้ — สั่งตรวจรอบนั้นใหม่เพื่อให้ได้ครบ
- ส่วนต่างทุกตัว **ระบบคำนวณเองจากคะแนนที่บันทึกไว้ทั้งสองฉบับ** ไม่ได้เชื่อคำบรรยายของ AI อย่างเดียว
- ป้าย "ตรวจครั้งที่ N" บนการ์ดคะแนนเป็นเพียง**ตัวนับ**ว่าฉบับนี้ถูกสั่งตรวจมาแล้วกี่ครั้ง ไม่ได้ใช้เทียบคะแนนข้ามครั้ง

---

## หน้าเว็บแบ่งเป็น 2 แท็บ และหน้าสรุปแยกอีก 1 หน้า

หน้า **ผู้ช่วย AI ตรวจเรียงความ** (`ai_feedback.php`) แบ่งเป็น

| แท็บ | มีอะไร |
|---|---|
| **ผลตรวจรายบุคคล** | ช่องเลือกนักเรียน · แถบสรุปย่อ (ตรวจแล้วกี่ฉบับ, เฉลี่ย, รอบล่าสุด, ผลเทียบร่างกี่คู่) · การ์ดคะแนนทุกรอบงาน · รายละเอียดของฉบับที่คลิก |
| **ภาพรวมผลตรวจ AI ทั้งชั้น** | ตัวเลขเด่นของทั้งชั้น · การ์ด **ค่าเฉลี่ยทั้งชั้นรายรอบงาน** · เครื่องมือของคุณครู (ตรวจทั้งรอบ / ตั้งค่า) · ตารางภาพรวมทั้งชั้น |

### ตัวกรองในตารางภาพรวมทั้งชั้น

ตารางภาพรวมกรองให้เหลือเฉพาะกลุ่มที่อยากดูได้ทันที (กรองจากคอลัมน์รอบงานที่กำลังแสดงอยู่)

| ตัวกรอง | คัดใครออกมา |
|---|---|
| **คะแนนต่ำลงกว่าฉบับตั้งต้น** | คนที่ร่างหลังได้คะแนนน้อยกว่าร่างก่อน (D1.2 ต่ำกว่า D1.1 · D2.2 ต่ำกว่า D2.1 · หลังเรียนต่ำกว่าก่อนเรียน) — กลุ่มที่ต้องเข้าไปดูก่อนใคร |
| **คะแนนเท่าเดิม ไม่ขยับ** | แก้ไปแล้วแต่คะแนนไม่ขยับเลย มักแปลว่ายังไม่ได้แก้ตรงจุด |
| **คะแนนดีขึ้น** | คนที่ร่างหลังดีขึ้นจริง ใช้หยิบตัวอย่างงานที่พัฒนาชัดมาให้ทั้งห้องดู |
| **ระดับคุณภาพ พอใช้ / ต้องปรับปรุง** | กลุ่มที่ยังอยู่ระดับล่าง ไม่ว่าจะเทียบร่างได้หรือไม่ |
| **ยังตรวจไม่ครบทุกรอบที่แสดงอยู่** | คนที่ยังขาดผลตรวจบางรอบ |
| **มีข้อที่ครูปรับคะแนนไว้** | ฉบับที่ผ่านการตรวจทานของครูรายข้อแล้ว |

ใช้ร่วมกับช่องค้นหา ช่องเลือกรอบงาน และกล่องติ๊ก "เฉพาะที่รอคะแนนจากครู / เฉพาะที่รอตรวจใหม่" ได้พร้อมกัน
ในช่องคะแนนแต่ละช่องมีสัญลักษณ์กำกับด้วย — ↗ เขียว = ดีขึ้น · ↘ แดง = ต่ำลง · ⊝ ส้ม = เท่าเดิม · ⚙️ ม่วง = ครูตรวจทานคะแนนไว้

ส่วน **สรุปภาพรวมผลงานเขียน** ของนักเรียนรายคน แยกออกไปเป็นหน้าของตัวเองที่ `ai_student_summary.php`
เปิดได้จากปุ่มในแท็บรายบุคคล หรือคลิกชื่อนักเรียนในตารางภาพรวม (เปิดในแท็บใหม่ของเบราว์เซอร์)
ในหน้านั้นมีการ์ดตัวเลขสรุป · **การ์ดคู่เทียบร่างทั้ง 3 คู่** · กราฟพัฒนาการ · กราฟรายเกณฑ์ ·
จุดแข็ง/จุดที่ต้องแก้ · เช็กลิสต์ก่อนเขียนครั้งถัดไป และปุ่มพิมพ์

---

## ภาพรวมการนำเสนอรายรอบงาน (ใช้เมื่อตรวจครบทั้งรอบแล้ว)

อยู่ในแท็บ **ภาพรวมผลตรวจ AI ทั้งชั้น** เลือกรอบงานแล้วกด **"ให้ AI เขียนภาพรวมรอบนี้"**
AI จะอ่านผลตรวจของนักเรียนทุกคนในรอบนั้นรวดเดียว แล้วเขียนให้ว่า

| ส่วน | บอกอะไร |
|---|---|
| ภาพรวมการนำเสนอ | ทั้งชั้นนำเสนอภาระงานนี้ไปในทิศทางใด มองประเด็นจากมุมไหนเป็นหลัก |
| **นักเรียนนำเสนอไปทางใดบ้าง** | แนวทาง/ประเด็นที่พบ พร้อมบอกว่ามีสัดส่วนเท่าไร (ส่วนใหญ่ / ครึ่งหนึ่ง / ส่วนน้อย) และยกตัวอย่างจากงานจริง |
| ประเด็นที่น่าสนใจ | สิ่งที่เหนือความคาดหมายหรือน่าหยิบไปคุยต่อในชั้นเรียน |
| **ข้อสังเกตต่อผลวิจัย** | ข้อสังเกตที่อ้างอิงตัวเลขจริงของรอบนั้น เช่น ร่างที่ 2 สูงกว่าร่างที่ 1 เฉลี่ยกี่คะแนน ดีขึ้นกี่ฉบับจากกี่ฉบับ |
| จุดที่ทั้งชั้นทำได้ดี / จุดบกพร่องที่พบซ้ำ | ใช้วางแผนสอนซ่อมเสริมได้ทันที |
| สิ่งที่ควรทำต่อในคาบถัดไป | ข้อเสนอแนะเชิงการสอนจากสิ่งที่เห็นในงานชุดนี้ |

**ตัวเลขทั้งหมดระบบคำนวณเอง ไม่ได้ให้ AI นับ** — จำนวนฉบับ ค่าเฉลี่ย การกระจายระดับคุณภาพ
คะแนนเฉลี่ยรายเกณฑ์ และส่วนต่างจากฉบับตั้งต้น ถูกคิดจากฐานข้อมูลแล้วส่งไปให้ AI ใช้อ้างอิง

> **ข้อสังเกตนี้ไม่ใช่ผลทดสอบทางสถิติ** — AI ถูกกำกับไว้ว่า **ห้ามสรุปว่า "มีนัยสำคัญ" หรือระบุค่า p / t เอง**
> ให้บรรยายได้เพียงทิศทางและขนาดของการเปลี่ยนแปลงตามตัวเลขที่ให้ไป
> การทดสอบนัยสำคัญจริงทำด้วย **Paired t-test** ในหน้า `research_analysis.php` — เวลารายงานผลวิจัยให้อ้างค่าจากหน้านั้น

เก็บไว้ในตาราง `essay_ai_phase_summary` (1 รอบงาน = 1 แถว เขียนใหม่ทับของเดิม)
ใช้โควตา 1 ครั้งต่อการเขียน 1 รอบ ครูเป็นผู้สั่งเท่านั้น ผู้เชี่ยวชาญเปิดอ่านได้

---

## AI ตรวจอะไรบ้าง

ใช้ **เกณฑ์เดียวกับแบบประเมินของคุณครู** ในหน้า "ประเมินให้คะแนน" ทั้งหมด 10 ข้อ:

| ด้าน | ข้อ | คะแนนเต็ม |
|---|---|---|
| 1) เนื้อหาสาระ | 1.1 ความตรงประเด็น · 1.2 แก่นเรื่องชัดเจน · 1.3 การขยายความและเหตุผล | 12 · 6 · 9 |
| 2) องค์ประกอบและการลำดับ | 2.1 ความครบถ้วนขององค์ประกอบ · 2.2 การลำดับประเด็นเป็นระบบ | 8 · 4 |
| 3) การใช้สำนวนภาษา | 3.1 การใช้ประโยคถูกต้อง · 3.2 การเลือกใช้คำ · 3.3 ระดับภาษาเหมาะสม | 4 · 6 · 5 |
| 4) อักขรวิธี | 4.1 การสะกดคำถูกต้อง · 4.2 การเว้นวรรค | 2 · 2 |

รวม **58 คะแนน** จากเต็ม 60 — ข้อ **4.3 ความเรียบร้อย (ลายมือ/ความสะอาด)**
AI ประเมินไม่ได้ เพราะต้องดูจากต้นฉบับที่นักเรียนเขียนด้วยลายมือ จึงเว้นไว้ให้ครูประเมินเอง

### ชนิดของงานเขียนในแต่ละรอบ — AI ตรวจตามชนิดของงาน ไม่ได้ตรวจแบบเดียวกันหมด

เกณฑ์ 10 ข้อข้างบนเป็นเกณฑ์กลางที่ใช้ได้กับเรียงความทุกชนิด แต่ภาระงานของคุณครูแต่ละรอบ
เป็นงานเขียนคนละชนิดกัน ระบบจึงบอก AI ไว้ในคำสั่งทุกครั้งว่ารอบนั้นเป็นงานชนิดใด
และชนิดนั้นมีผลกับข้อ **1.1 · 1.2 · 1.3** อย่างไร (ป้ายบอกชนิดงานขึ้นบนการ์ดคะแนนแต่ละใบด้วย)

| รอบงาน | ชนิดงานเขียน | สิ่งที่ต้องมีจึงจะได้คะแนนดี |
|---|---|---|
| ภาระงานหน่วยที่ 1 (D1.1 · D1.2) | **เรียงความเชิงบรรยาย** | ต้องกล่าวถึงคำสำคัญครบทั้ง 2 คำ — **ปัญญาประดิษฐ์** และ **การเรียนรู้ตลอดชีวิต** — และต้อง **เชื่อมโยงสองเรื่องเข้าด้วยกัน** ไม่ใช่พูดถึงแยกกันคนละส่วน พร้อมบรรยายด้วยรายละเอียด/ตัวอย่างที่เห็นภาพ |
| ภาระงานหน่วยที่ 2 (D2.1 · D2.2) · ก่อนเรียน · หลังเรียน | **เรียงความเชิงวิจารณ์** | ต้องมีครบ 4 อย่าง: ประเด็นที่ยกมาวิจารณ์ · ทรรศนะ/จุดยืนของผู้เขียน · การประเมินค่าพร้อมเหตุผลและตัวอย่าง · ข้อสรุปเชิงวิจารณ์ |

กติกาที่ระบบกำกับ AI ไว้:

- **หน่วยที่ 1** — ต้องไล่ยกข้อความจริงมาดูทีละคำสำคัญก่อนให้คะแนน · ขาดไป 1 คำ หรือกล่าวถึงผ่าน ๆ
  โดยไม่มีเนื้อหารองรับ ให้ลดระดับข้อ 1.1 ตามจริง · ขาดทั้งสองคำ = ไม่สัมพันธ์กับหัวข้อ ·
  แก่นเรื่อง (1.2) ต้องเป็น "ความสัมพันธ์ของสองคำนี้" · การบอกแค่นิยามของคำ ไม่นับเป็นการขยายความ (1.3)
- **งานเชิงวิจารณ์** — งานที่เล่าหรือบรรยายล้วน ๆ ไม่มีทรรศนะและการประเมินค่าเลย ถือว่ายังไม่ตรง
  ลักษณะงานที่ครูกำหนด ต้องลดระดับ 1.1 ตามจริง · แก่นเรื่อง (1.2) คือทรรศนะหลักของผู้เขียน ·
  ทุกทรรศนะต้องมีเหตุผล/ตัวอย่างรองรับ (1.3) · **ห้ามหักคะแนนเพราะจุดยืนของนักเรียนต่างจากของ AI**
  — วิจารณ์ทางไหนก็ได้ ขอให้มีเหตุผลรองรับ
- ทั้งสองชนิด ถ้ายังขาดสิ่งที่กำหนด AI ต้องเขียนบอกไว้ใน "จุดที่ควรปรับปรุง" เป็นข้อแรก ๆ

> เปลี่ยนหัวข้อหรือชนิดงานในปีถัดไป: แก้ที่ตาราง `ai_essay_styles()` ในไฟล์ `ai_config.php` ที่เดียว
> (คีย์คือรอบหัวข้อ `pretest` / `task1` / `task2` / `posttest`)

### ฉบับตั้งต้น (ก่อนเรียน · D1.1 · D2.1) ตรวจเข้มตามความเป็นจริง

คะแนนของสามรอบนี้เป็น **จุดตั้งต้นของการวัดพัฒนาการ** ถ้าให้เกินจริง ร่างที่นักเรียนแก้มา
ในรอบถัดไปจะดูเหมือนไม่พัฒนาทั้งที่พัฒนาขึ้นจริง ระบบจึงกำกับ AI ไว้ว่า

- ตัดสินทุกข้อจาก **หลักฐานที่นับได้ในงานจริง** เทียบกับคำบรรยายของแต่ละระดับตรง ๆ
  (เกณฑ์ข้อใดบอกจำนวน เช่น "ผิดได้ไม่เกิน 2 แห่ง" ให้นับของจริงก่อนแล้วจึงเทียบระดับ)
- **ห้ามปัดคะแนนขึ้นให้** เพราะความตั้งใจ ความยาว ความสวยของสำนวน หรือเพราะเป็นงานชิ้นแรก ·
  ระดับ 4 = ไม่มีข้อบกพร่องในข้อนั้นเลย
- เหตุผลรายข้อ (`score_reasons`) ต้องบอกหลักฐานที่นับได้ เช่น *"พบภาษาพูด 3 ตำแหน่ง ได้แก่ …"*
  ห้ามเขียนลอย ๆ ว่าเขียนดีหรือไม่ดี

### ความยาว 250-300 คำ เป็นแนวทาง ไม่ใช่เกณฑ์ให้คะแนน

ระบบบอกจำนวนคำให้ AI รู้ แต่กำกับไว้ชัดเจนว่า **ห้ามหักคะแนนข้อใดเพราะเขียนยาวเกิน 300 คำ**
และห้ามยกเรื่องความยาวขึ้นมาเป็นจุดที่ควรปรับปรุง ถ้าเนื้อหายังอยู่ในขอบเขตของหัวข้อและมีสาระตลอด
ให้ประเมินตามคุณภาพเนื้อหาไปตามปกติ

AI จะหักคะแนนที่เกี่ยวกับความยาวได้เพียง 2 กรณีเท่านั้น
คือส่วนที่ยาวขึ้นเป็น**เนื้อหานอกขอบเขตหัวข้อ** (หักที่ข้อ 1.1 ความตรงประเด็น)
หรือเป็นการ**เขียนวนซ้ำไม่เพิ่มสาระ** (หักที่ข้อ 3.2 การเลือกใช้คำ) — ทั้งสองกรณีเป็นปัญหาที่ตัวเนื้อหา
ไม่ใช่ที่จำนวนคำ ส่วนมาตรวัดความยาวในหน้าเขียนเรียงความของนักเรียนยังทำงานเหมือนเดิม
เพราะเป็นแนวทางให้นักเรียนคุมความยาว ไม่ได้เอาไปคิดคะแนน

### คะแนนข้อ 4.3 ดึงมาจากแบบประเมินให้อัตโนมัติ

ถ้าคุณครูให้คะแนนข้อ 4.3 ไว้แล้วในหน้า **ประเมินให้คะแนน** (`evaluation.php`) ระบบจะ
**ดึงคะแนนนั้นมาใช้ในหน้าผู้ช่วย AI ให้เลย** ไม่ต้องกรอกซ้ำสองที่ — คะแนนรวมจึงครบเต็ม 60
และป้าย "รอคะแนนครู" จะหายไปเอง

| กรณี | ระบบใช้คะแนนจากไหน |
|---|---|
| ยังไม่ได้กรอกในหน้าผู้ช่วย AI แต่มีในแบบประเมินแล้ว | **แบบประเมิน** (แสดงป้าย "จากแบบประเมิน" พร้อมชื่อผู้ประเมินและเวลา) |
| กรอกไว้ในหน้าผู้ช่วย AI แล้ว | **ค่าที่กรอกในหน้าผู้ช่วย AI** (ทับค่าจากแบบประเมิน) |
| ล้างค่าที่กรอกในหน้าผู้ช่วย AI | กลับไปใช้ค่าจาก**แบบประเมิน** อีกครั้ง |

การจับคู่รอบงาน: ก่อนเรียน↔ก่อนเรียน · หน่วยที่ 1 ร่างที่ 2↔หน่วยที่ 1 · หน่วยที่ 2 ร่างที่ 2↔หน่วยที่ 2 ·
หลังเรียน↔หลังเรียน — **ร่างที่ 1 ของทั้งสองหน่วยไม่มีคะแนนให้ดึง** เพราะคุณครูให้คะแนนเฉพาะร่างที่ 2
ถ้ามีการประเมินหลายครั้งในรอบเดียวกัน ระบบใช้ครั้งล่าสุด

### อ่านเทียบสองฉบับเต็ม ๆ ทีละส่วน

ในกล่องเทียบร่างมีปุ่ม **"อ่านเทียบ … เต็ม ๆ ทีละส่วน"** กดแล้วจะกางข้อความของทั้งสองฉบับ
มาวางคู่กัน ซ้าย = ฉบับตั้งต้น ขวา = ฉบับนี้ แยกทีละส่วน (คำนำ · เนื้อเรื่องแต่ละย่อหน้า · สรุป)
พร้อมจำนวนคำของแต่ละส่วนว่าเปลี่ยนจากเท่าไรเป็นเท่าไร

- วรรคที่ **หายไป** จากฉบับเดิม ขึ้นพื้นแดงขีดฆ่าทางซ้าย
- วรรคที่ **เพิ่มเข้ามา** ในฉบับใหม่ ขึ้นพื้นเขียวทางขวา
- ย่อหน้าที่เพิ่มมาทั้งย่อหน้าหรือถูกตัดทิ้งทั้งย่อหน้า ใช้ป้ายสถานะบอกแทน ไม่ระบายทั้งย่อหน้าให้อ่านยาก
- **ระบบเทียบข้อความเอง ไม่ได้ถาม AI** จึงยืนยันได้ว่าส่วนที่ขึ้นว่า "ไม่ได้แก้" คือเหมือนเดิมทุกตัวอักษรจริง

คู่ **ก่อนเรียน ↔ หลังเรียน** เป็นคนละหัวข้อ จึงวางคู่กันให้อ่านเทียบสำนวนเฉย ๆ ไม่ทำเครื่องหมายว่าเปลี่ยนตรงไหน
เพราะเป็นงานเขียนคนละชิ้น การไฮไลต์ว่า "วรรคนี้เพิ่มเข้ามา" จะไม่มีความหมาย

---

## ไม่เห็นด้วยกับคะแนนของ AI — ปรับรายข้อได้

คะแนนของ AI เป็น **ข้อมูลตั้งต้น** ไม่ใช่คำตัดสินสุดท้าย ในตาราง "คะแนนรายเกณฑ์"
ทุกแถวมีปุ่ม ⚙️ ท้ายแถว กดแล้วเลือกได้ 2 ทาง

### ทางที่ 1 — คุณครูปรับคะแนนข้อนั้นเอง

เลือกระดับคะแนนดิบ 0-4 ที่เห็นว่าเหมาะสม (มีคำอธิบายเกณฑ์รายระดับให้อ่านประกอบ)
แล้วเขียน **เหตุผลที่ปรับ** กำกับไว้ เหตุผลจะแสดงคู่กับคะแนนเสมอ

### ทางที่ 2 — สั่งให้ AI ตรวจเฉพาะข้อนั้นใหม่ (ใส่คำสั่งเพิ่มเติมได้)

AI จะอ่านเรียงความทั้งฉบับอีกครั้ง แต่ให้คะแนน **เฉพาะข้อนั้นข้อเดียว** ข้ออื่นไม่ขยับ
และมีช่อง "คำสั่งเพิ่มเติมถึง AI" ให้พิมพ์สิ่งที่อยากให้พิจารณาเพิ่ม เช่น

> *ให้ดูย่อหน้าที่ 3 ด้วย นักเรียนยกตัวอย่างไว้แล้ว*
> *ข้อนี้ให้นับเฉพาะคำเชื่อมที่ใช้ผิด ไม่ต้องนับคำซ้ำ*

ระบบ **ไม่บอกคะแนนเดิมให้ AI รู้** เพื่อให้ตรวจใหม่แบบสด ๆ ไม่ถูกคะแนนเดิมตรึงไว้
และกำกับไว้ในคำสั่งว่าถ้าคำสั่งของครูขัดกับสิ่งที่ปรากฏในเรียงความ ให้ยึดเรียงความเป็นหลัก
คำสั่งที่พิมพ์ไปถูกบันทึกไว้ให้ตรวจสอบย้อนหลังได้ · ใช้โควตา 1 ครั้งเท่าการตรวจทั้งฉบับ
แต่คำสั่งสั้นกว่ามาก จึงเร็วกว่า (ราว 5-15 วินาที)

### คะแนนเดิมของ AI ไม่เคยถูกทับ

| สิ่งที่เก็บ | อยู่ที่ไหน |
|---|---|
| คะแนนที่ AI ให้ครั้งแรก | คอลัมน์ `scores` — **ไม่เคยถูกแก้** |
| คะแนนที่ปรับ + เหตุผล + ผู้ปรับ + เวลา + คำสั่งที่ให้ AI | คอลัมน์ `score_overrides` (แยกอีกคอลัมน์) |

ในตารางคะแนน แถวที่ถูกปรับจะขึ้นพื้นสีม่วงอ่อน แสดงคะแนนเดิมขีดฆ่าคู่กับคะแนนที่ใช้จริง
พร้อมป้ายว่า "ครูปรับคะแนน" หรือ "AI ตรวจข้อนี้ใหม่" และกด **คืนค่าคะแนนของ AI** ได้ตลอด

**คะแนนที่ใช้จริง (หลังครูตรวจทาน) คือคะแนนที่ไปปรากฏในทุกที่** — คะแนนรวม ระดับคุณภาพ
ตารางเทียบร่าง ตารางภาพรวมทั้งชั้น และรายงานบทที่ 4-5 ส่วนคะแนนดิบของ AI ยังอ่านย้อนหลังได้
จากฐานข้อมูลเสมอ จึงรายงานได้ว่าครูตรวจทานไปกี่ข้อ ต่างจาก AI เท่าไร

> ⚠️ **การกด "ให้ AI ตรวจใหม่ทั้งฉบับ" จะล้างคะแนนที่ปรับไว้ทั้งหมด** เพราะเป็นการอ่านงานใหม่หมดทุกข้อ
> ระบบจะเตือนก่อนเสมอเมื่อฉบับนั้นมีคะแนนที่ปรับไว้ · ถ้าอยากแก้แค่บางข้อ ให้ใช้ปุ่ม ⚙️ ท้ายแถวแทน

---

## ขีดจำกัดและการป้องกันการใช้เกินโควตา

ระบบใส่กันชนไว้แล้วเพื่อไม่ให้โควตาฟรีหมดเร็วเกินไป:

| รายการ | ค่าที่ตั้งไว้ |
|---|---|
| ครู ตรวจได้สูงสุด | **400 ครั้ง/วัน (ค่าเริ่มต้น — ครูปรับเองได้ 50-5,000)** |
| เรียงความต้องยาวอย่างน้อย | 40 คำ |
| ความยาวสูงสุดที่ส่งให้ AI | 12,000 ตัวอักษร |
| เว้นจังหวะระหว่างฉบับตอนตรวจทั้งรอบ | 2 วินาที |

400 ครั้ง/วันเพียงพอสำหรับตรวจทั้งชั้น 39 คน ครบทั้ง 6 รอบในวันเดียว (234 ครั้ง)
แล้วยังเหลือสำหรับตรวจซ้ำบางฉบับ

**ถ้าโควตาไม่พอ** (เช่นมีนักเรียนหลายห้อง หรืออยากตรวจใหม่ทั้งชุดหลายรอบในวันเดียว)
เข้าหน้า **ผู้ช่วย AI ตรวจงาน** → การ์ด **ตั้งค่าผู้ช่วย AI** → ช่อง **โควตาการตรวจต่อวัน**
ใส่ตัวเลขใหม่แล้วกดบันทึก (ตั้งได้ 50-5,000 ครั้ง/วัน)

> ⚠️ เพดานนี้เป็นของ **ระบบเรา** ไว้กันเผลอสั่งตรวจรัวจนโควตาฟรีหมดโดยไม่รู้ตัว
> **ผู้ให้บริการ AI ยังมีเพดานของตัวเองอีกชั้นหนึ่ง** — ตั้งเลขสูงกว่าที่เขาให้ ก็ไม่ทำให้ตรวจได้เกินของเขา
> (ถ้าชนเพดานของผู้ให้บริการ จะขึ้นข้อความว่า *ใช้โควตาฟรีของผู้ให้บริการครบแล้ว*)
>
> โควตานับ **เฉพาะครั้งที่ตรวจสำเร็จ** และรีเซ็ตทุกเที่ยงคืน — ครั้งที่ตรวจไม่ผ่านไม่กินโควตา

แก้ *ค่าเริ่มต้น* ของค่าเหล่านี้ได้ที่ค่าคงที่ด้านบนของไฟล์ `ai_config.php`
(`AI_DAILY_LIMIT_TEACHER`, `AI_DAILY_LIMIT_MIN`, `AI_DAILY_LIMIT_MAX`, `AI_MIN_WORDS`, `AI_MAX_CHARS`)

ครูดูสถิติการเรียกใช้ย้อนหลัง 7 วันได้ท้ายการ์ด "ตั้งค่าผู้ช่วย AI"

---

## แก้ปัญหาที่พบบ่อย

| อาการ | สาเหตุ / วิธีแก้ |
|---|---|
| ขึ้นว่า *ยังไม่ได้ตั้งค่า AI* | ยังไม่ได้ใส่ API key — ทำตามขั้นที่ 2 |
| *API key ไม่ถูกต้องหรือหมดสิทธิ์ใช้งาน* | คีย์ผิด/ถูกยกเลิก → สร้างคีย์ใหม่แล้ววางใหม่ |
| *ใช้โควตาฟรีของผู้ให้บริการครบแล้ว* | รอสักครู่แล้วลองใหม่ หรือเปลี่ยนไปใช้ผู้ให้บริการอีกเจ้า |
| *ไม่พบโมเดลที่ตั้งค่าไว้* | ชื่อโมเดลพิมพ์ผิด หรือผู้ให้บริการเลิกให้บริการรุ่นนั้นแล้ว → กดปุ่ม **ใช้ค่าเริ่มต้น** ข้างช่องชื่อโมเดล แล้วกด **บันทึกการตั้งค่า** (ถ้าข้อความ error บอกชื่อรุ่นใหม่มาด้วย จะพิมพ์ชื่อนั้นลงไปตรง ๆ ก็ได้) |
| *เซิร์ฟเวอร์เรียก API ภายนอกไม่ได้* | โฮสต์ปิด cURL และ `allow_url_fopen` → แจ้งผู้ดูแลเซิร์ฟเวอร์ให้เปิด **cURL extension** |
| *อ่านคำตอบของ AI ไม่ได้ (ไม่ใช่ JSON ที่สมบูรณ์)* | ข้อความแจ้งเตือนจะแสดงคำตอบที่ AI ส่งมาให้ดูด้วย → ถ้าเป็นคำปฏิเสธ แปลว่าโมเดลไม่ยอมตอบ ให้กดตรวจใหม่หรือเปลี่ยนโมเดล · ถ้าคำตอบดูเหมือนถูกตัดกลางคัน ระบบจะพยายามกู้ให้อัตโนมัติอยู่แล้ว |
| ตรวจแล้วค้างนาน / หมดเวลา | โฮสต์จำกัดเวลาทำงานของ PHP → ให้ผู้ดูแลเพิ่ม `max_execution_time` เป็น 120 วินาที |

---

## ฐานข้อมูลที่ระบบสร้างเพิ่ม

สร้างให้อัตโนมัติเมื่อเปิดเว็บครั้งแรกหลังอัปเดต (ดูโค้ดใน `db_config.php`)
หากต้องการรันเองผ่าน phpMyAdmin ให้ใช้ไฟล์ `migration_ai_feedback.sql`

| ตาราง | เก็บอะไร |
|---|---|
| `app_settings` | ค่าตั้งค่า AI ที่ครูกรอกในเว็บ |
| `essay_ai_feedback` | ผลตรวจของ AI (นักเรียน 1 คน ต่อ 1 รอบ = 1 แถว ตรวจใหม่ทับของเดิม) พร้อมคะแนนที่ครูให้เอง, คะแนนที่ครูตรวจทานรายข้อ (`score_overrides`), สถานะคิวตรวจใหม่ (`essay_hash`, `recheck_needed`, `recheck_marked_at`) ตัวนับการตรวจ (`review_round`) และข้อมูลเทียบกับฉบับตั้งต้นตามคู่ที่ครูกำหนด (`baseline_phase`, `baseline_snapshot`, `draft_comment`, `draft_changes`) |
| `essay_ai_phase_summary` | ภาพรวมการนำเสนอรายรอบงานที่ AI เขียนให้ (1 รอบงาน = 1 แถว) |
| `ai_usage_log` | บันทึกการเรียกใช้ AI สำหรับจำกัดโควตาและตรวจสอบย้อนหลัง |

---

## ไฟล์ที่เกี่ยวข้อง

| ไฟล์ | หน้าที่ |
|---|---|
| `ai_config.php` | ตั้งค่า AI, เกณฑ์ที่ส่งให้ AI, การเรียก API, การแปลงคำตอบ |
| `ai_feedback.php` | หน้าเว็บผู้ช่วย AI (ครู / นักเรียน / ผู้เชี่ยวชาญ) — 2 แท็บ: รายบุคคล / ภาพรวมทั้งชั้น |
| `ai_student_summary.php` | หน้า "สรุปภาพรวมผลงานเขียน" ของนักเรียนรายคน (เปิดแยกเป็นอีกหน้าหนึ่ง) |
| `ai_review.js` | ตัวช่วยฝั่งหน้าเว็บ ใช้ร่วมกับหน้าเขียนเรียงความ รวมถึงกล่องเทียบร่าง |
| `ai_summary.js` | ตัวสร้างหน้าสรุปภาพรวมผลงานเขียนรายบุคคล (กราฟ · การ์ดคู่เทียบ · จุดที่ต้องแก้) |
| `ai_secrets.sample.php` | ตัวอย่างไฟล์เก็บ API key บนเซิร์ฟเวอร์ |
| `migration_ai_feedback.sql` | สคริปต์สร้างตาราง (กรณีรันเอง) |
