# คู่มือติดตั้งและใช้งานระบบ OCR (Unlimited-OCR) ในหน้า "บันทึกเรียงความ"

คู่มือนี้อธิบายตั้งแต่ "ระบบทำงานยังไง" ไปจนถึง "ตั้งค่าเสร็จแล้วใช้งานจริง" แบบทีละขั้น
เขียนสำหรับคุณครูผู้ดูแลระบบ ThaiEasay โดยเฉพาะ

---

## 1. ระบบทำงานอย่างไร (ภาพรวมสำคัญมาก)

โมเดล **baidu/Unlimited-OCR** เป็นโมเดล AI ขนาดใหญ่ที่ **ต้องรันบนเครื่องที่มีการ์ดจอ (GPU)**
เว็บ PHP บนโฮสต์ฟรี (InfinityFree) **รันโมเดลเองไม่ได้** ทำได้แค่ "ส่งรูปไปให้เครื่อง GPU อ่าน แล้วรับข้อความกลับ"

```
[นักเรียนถ่ายรูปเรียงความ]
        │  (อัปโหลดผ่านเบราว์เซอร์)
        ▼
[เว็บ ThaiEasay / api.php]  ── ส่งรูป (base64) ──►  [เซิร์ฟเวอร์ GPU ที่รัน Unlimited-OCR]
        ▲                                                    │
        └──────────  รับข้อความที่ถอดได้กลับมา  ◄────────────┘
        │
        ▼
[เติมลงช่อง คำนำ / เนื้อเรื่อง / สรุป อัตโนมัติ]
```

**สรุปสิ่งที่ต้องมี 2 ส่วน:**
1. **เซิร์ฟเวอร์ GPU** ที่รันโมเดล Unlimited-OCR (ส่วนที่ยากและมีค่าใช้จ่าย)
2. **การตั้งค่าในเว็บ** ให้ชี้ไปที่เซิร์ฟเวอร์นั้น (ส่วนนี้ง่าย ทำเสร็จแล้วในโค้ด)

> ⚠️ **ข้อเท็จจริงที่ต้องรู้ก่อน:** ถ้าไม่มีเครื่อง GPU / เซิร์ฟเวอร์ที่รันโมเดลอยู่ ปุ่ม OCR จะขึ้น
> ข้อความว่า "เชื่อมต่อเซิร์ฟเวอร์ OCR ไม่ได้" เสมอ — เว็บจะไม่พัง แต่ฟีเจอร์ OCR จะยังใช้ไม่ได้
> จนกว่าจะทำข้อ 2 เสร็จ

---

## 2. ตั้งเซิร์ฟเวอร์ OCR (เลือก 1 วิธี)

Unlimited-OCR ต้องการ GPU ที่มี VRAM สูง (แนะนำ 24 GB ขึ้นไป เช่น RTX 3090 / 4090 / A5000 / A100)

### วิธีที่ A — เช่า GPU คลาวด์ (แนะนำสำหรับโรงเรียนที่ไม่มีเครื่อง GPU)

บริการที่นิยมและคิดเงินเป็นชั่วโมง เช่น **RunPod**, **Vast.ai**, **Google Colab Pro** (บางแพ็กเกจ)

ขั้นตอนบน RunPod (ตัวอย่าง):
1. สมัคร https://www.runpod.io แล้วเติมเงิน
2. เลือก **Deploy a Pod** → เลือก GPU เช่น RTX 4090 (24GB) หรือ A100
3. เลือก template ที่มี CUDA + Python (เช่น "RunPod PyTorch")
4. เปิด SSH / Web Terminal เข้าไปในเครื่อง แล้วรันคำสั่งในข้อ 2.1

### วิธีที่ B — ใช้เครื่อง GPU ของโรงเรียน/ที่บ้านเอง

ต้องมี: การ์ดจอ NVIDIA + ติดตั้ง CUDA + Python 3.12 เรียบร้อย แล้วรันคำสั่งในข้อ 2.1
(เครื่องต้องเปิดทิ้งไว้ตลอดเวลาที่ต้องการให้นักเรียนใช้ OCR ได้)

### 2.1 คำสั่งรันเซิร์ฟเวอร์โมเดล (SGLang — วิธีที่ทางการแนะนำ)

ติดตั้งและรัน (รันบนเครื่อง GPU):

```bash
# 1) ติดตั้ง SGLang
pip install "sglang[all]"

# 2) รันเซิร์ฟเวอร์โมเดล (จะดาวน์โหลดโมเดลอัตโนมัติครั้งแรก อาจใช้เวลานาน)
python -m sglang.launch_server \
    --model baidu/Unlimited-OCR \
    --served-model-name Unlimited-OCR \
    --attention-backend fa3 \
    --page-size 1 \
    --mem-fraction-static 0.8 \
    --context-length 32768 \
    --enable-custom-logit-processor \
    --disable-overlap-schedule \
    --skip-server-warmup \
    --host 0.0.0.0 \
    --port 10000
```

เมื่อรันสำเร็จ เซิร์ฟเวอร์จะเปิด API แบบ OpenAI-compatible ที่:
```
http://<ไอพีเครื่อง>:10000/v1/chat/completions
```

> ทางเลือก: ถ้าใช้ **vLLM ผ่าน Docker** ก็ได้ (`docker pull vllm/vllm-openai:unlimited-ocr`)
> แต่ SGLang ด้านบนตั้งค่าง่ายกว่าและเป็นคำสั่งที่ repo ให้มาโดยตรง

### 2.2 ทดสอบว่าเซิร์ฟเวอร์อ่านรูปได้จริง (บนเครื่อง GPU)

```bash
# แปลงรูปเป็น base64 แล้วยิงทดสอบ
IMG=$(base64 -w0 test.jpg)
curl http://127.0.0.1:10000/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{
    "model": "Unlimited-OCR",
    "messages": [{"role":"user","content":[
      {"type":"text","text":"document parsing."},
      {"type":"image_url","image_url":{"url":"data:image/jpeg;base64,'"$IMG"'"}}
    ]}]
  }'
```

ถ้าได้ข้อความ (`choices[0].message.content`) กลับมา = เซิร์ฟเวอร์พร้อมใช้งาน ✅

---

## 3. ทำให้เว็บเข้าถึงเซิร์ฟเวอร์ได้ (สำคัญ)

เว็บ ThaiEasay อยู่บนอินเทอร์เน็ต (InfinityFree) แต่เซิร์ฟเวอร์ GPU มักอยู่หลังเราเตอร์/ไม่มี IP สาธารณะ
ต้องทำให้ port 10000 เข้าถึงได้จากภายนอกก่อน เลือกวิธีใดวิธีหนึ่ง:

- **RunPod/Vast.ai:** เปิด "Expose HTTP Port 10000" ในหน้าตั้งค่า Pod → ระบบจะให้ URL สาธารณะมา
  เช่น `https://xxxx-10000.proxy.runpod.net`
- **เครื่องตัวเอง + Cloudflare Tunnel (ฟรี):**
  ```bash
  cloudflared tunnel --url http://localhost:10000
  ```
  จะได้ URL แบบ `https://random-name.trycloudflare.com`
- **เครื่องตัวเอง + ngrok:** `ngrok http 10000` → ได้ URL สาธารณะเช่นกัน

> 🔒 แนะนำให้ endpoint เป็น **HTTPS** เพราะเว็บ ThaiEasay รันบน HTTPS (เบราว์เซอร์อาจบล็อกการเรียก HTTP ที่ไม่ปลอดภัย)

จด URL สาธารณะที่ได้ไว้ เช่นสมมติได้ว่า:
```
https://xxxx-10000.proxy.runpod.net/v1/chat/completions
```

---

## 4. ตั้งค่าในเว็บ ThaiEasay (ให้ชี้ไปที่เซิร์ฟเวอร์)

มี 3 ค่าให้ตั้ง:

| ชื่อค่า | ความหมาย | ตัวอย่าง |
|---------|----------|----------|
| `OCR_API_URL` | URL endpoint แบบ OpenAI-compatible | `https://xxxx-10000.proxy.runpod.net/v1/chat/completions` |
| `OCR_MODEL`   | ชื่อโมเดล (ต้องตรงกับ `--served-model-name`) | `Unlimited-OCR` |
| `OCR_API_KEY` | คีย์ (ถ้าเซิร์ฟเวอร์ไม่ได้ตั้งไว้ ใส่อะไรก็ได้) | `EMPTY` |

**วิธีตั้งค่า — เลือก 1 วิธี:**

### วิธีที่ 1: แก้ไฟล์ `db_config.php` โดยตรง (ง่ายสุดบน InfinityFree)

เปิดไฟล์ `db_config.php` หาบล็อก "ตั้งค่าเซิร์ฟเวอร์ OCR" แล้วเปลี่ยนค่า default:

```php
define('OCR_API_URL', $ocr_url !== false && $ocr_url !== ''
    ? $ocr_url
    : 'https://xxxx-10000.proxy.runpod.net/v1/chat/completions');   // ◄─ ใส่ URL ของคุณ
...
define('OCR_MODEL', $ocr_model !== false && $ocr_model !== ''
    ? $ocr_model
    : 'Unlimited-OCR');                                              // ◄─ ให้ตรงกับ served-model-name
```

### วิธีที่ 2: ตั้งเป็น Environment Variable (ถ้าโฮสต์รองรับ)

ตั้งค่า `OCR_API_URL`, `OCR_MODEL`, `OCR_API_KEY` ใน environment ของเซิร์ฟเวอร์เว็บ
(โค้ดจะอ่านค่าจาก env ก่อนเสมอ ไม่ต้องแก้ไฟล์) — เหมาะกับโฮสต์ที่มีแผงตั้งค่า env

---

## 5. วิธีใช้งานจริง (ฝั่งนักเรียน)

1. เข้าเมนู **"บันทึกเรียงความ"** (`essay_writer.php`)
2. เลือกหน่วย/รอบที่ต้องการบันทึก (ก่อนเรียน / หน่วย 1 / หน่วย 2 / หลังเรียน)
3. ที่การ์ด **"📷 อ่านข้อความจากรูปภาพ (OCR)"** กด **"เปิดใช้งาน"**
4. กดเลือกไฟล์ หรือถ่ายรูปเรียงความบนกระดาษ (บนมือถือจะเปิดกล้องให้)
5. กด **"อ่านข้อความจากรูป"** → รอสักครู่ ข้อความจะขึ้นในกล่องด้านขวา
6. **ตรวจทาน/แก้ไข** ข้อความให้ถูกต้อง (OCR อาจอ่านลายมือผิดได้บ้าง)
7. กด **"เติมอัตโนมัติ (แบ่ง คำนำ/เนื้อเรื่อง/สรุป)"**
   - ระบบแบ่งย่อหน้าแรก = คำนำ, ย่อหน้าสุดท้าย = สรุป, ที่เหลือ = เนื้อเรื่อง
   - หรือใช้ปุ่มใส่เฉพาะช่อง (คำนำ / เนื้อเรื่อง / สรุป) ก็ได้
8. ตรวจความถูกต้องอีกครั้ง แล้วกด **"บันทึกเรียงความ"** ตามปกติ

---

## 6. แก้ปัญหาที่พบบ่อย

| อาการ | สาเหตุ / วิธีแก้ |
|-------|------------------|
| "เชื่อมต่อเซิร์ฟเวอร์ OCR ไม่ได้" | เซิร์ฟเวอร์ GPU ไม่ได้รัน / URL ผิด / ยังไม่ได้ expose port → ตรวจข้อ 2–4 |
| "เซิร์ฟเวอร์ OCR ตอบกลับสถานะ 404/500" | `OCR_MODEL` ไม่ตรงกับ `--served-model-name` หรือ URL ไม่ได้ลงท้าย `/v1/chat/completions` |
| เบราว์เซอร์บล็อก (mixed content) | endpoint เป็น HTTP แต่เว็บเป็น HTTPS → ใช้ HTTPS (Cloudflare Tunnel/ngrok ช่วยได้) |
| อ่านช้ามาก / timeout | รูปใหญ่เกินไปหรือ GPU ช้า → ลดขนาดรูป หรือใช้ GPU แรงขึ้น (โค้ดตั้ง timeout ไว้ 120 วินาที) |
| อ่านลายมือไทยเพี้ยน | เป็นข้อจำกัดของโมเดล ให้นักเรียนตรวจทานก่อนบันทึกเสมอ / ถ่ายรูปให้ชัด แสงพอ ตัวหนังสือตรง |

---

## 7. หมายเหตุเรื่องค่าใช้จ่ายและทางเลือก

- GPU คลาวด์คิดเงินเป็นชั่วโมง (ประมาณ 10–40 บาท/ชม. ขึ้นกับรุ่น) — **เปิดเฉพาะช่วงที่ให้นักเรียนใช้** เพื่อประหยัด
- ถ้าต้องการใช้ตลอดเวลาโดยไม่ดูแลเซิร์ฟเวอร์เอง อาจพิจารณาบริการ OCR แบบ API สำเร็จรูปอื่น
  (เช่น Google Cloud Vision, Typhoon OCR) แล้วปรับ `api.php` action `ocr_essay` ให้เรียก API นั้นแทน
  — แจ้งผู้พัฒนาได้หากต้องการให้ปรับให้

---

*ส่วนของเว็บ (ปุ่ม/หน้าจอ/ตัวกลางเรียก API) ติดตั้งเรียบร้อยแล้วในโค้ด เหลือเพียงตั้งเซิร์ฟเวอร์ OCR ตามข้อ 2–4*
